# Быстрый старт: Инструменты для проверки качества

Этот документ содержит краткое описание новых инструментов для автоматизации проверок и повышения качества кода.

## 🚀 Быстрая проверка сервисов

### Команда
```powershell
.\scripts\quick-check.ps1 [backend|frontend|parser|all]
```

### Примеры
```powershell
# Проверить все сервисы
.\scripts\quick-check.ps1 all

# Только Backend
.\scripts\quick-check.ps1 backend

# Только Frontend
.\scripts\quick-check.ps1 frontend
```

### Что проверяется
- **Backend**: импорты, health endpoint
- **Frontend**: линтер, доступность
- **Parser Service**: health endpoint, Chrome CDP

## 🔒 Pre-commit hook

### Установка
```powershell
.\scripts\setup-pre-commit.ps1
```

### Что проверяется автоматически
- Импорты Backend
- Линтер Frontend

### Отключение (временно)
```bash
git commit --no-verify
```

## 🧪 Тестирование гипотез

### Шаблон
`temp/templates/test_hypothesis_template.py`

### Использование
1. Скопируйте шаблон:
   ```powershell
   copy temp\templates\test_hypothesis_template.py temp\test_my_hypothesis.py
   ```
2. Заполните TODO комментарии
3. Запустите:
   ```powershell
   python temp\test_my_hypothesis.py
   ```

### Пример использования
```python
# temp/test_blacklist_hypothesis.py
def test_hypothesis():
    # Проверка добавления домена в blacklist
    response = requests.post(
        f"{BASE_URL}/moderator/blacklist",
        json={"domain": "test.ru"}
    )
    assert response.status_code == 201
    # Проверка фильтрации
    # ...
```

## 📝 Документирование ошибок

### Шаблон
`temp/templates/troubleshooting_template.md`

### Использование
1. Откройте шаблон
2. Заполните все поля
3. Скопируйте в `docs/TROUBLESHOOTING.md`

### Важно
Каждая ошибка ДОЛЖНА быть задокументирована в `docs/TROUBLESHOOTING.md`

## 📚 Дополнительная документация

- **Полные рекомендации**: `docs/WORKFLOW_IMPROVEMENTS.md`
- **Правила работы**: `.cursorrules`
- **Библия ошибок**: `docs/TROUBLESHOOTING.md`

## ✅ Чеклист перед коммитом

- [ ] Запустить `.\scripts\quick-check.ps1 all`
- [ ] Проверить, что нет ошибок линтера
- [ ] Проверить через реальный фронтенд (если применимо)
- [ ] Задокументировать ошибки (если были)

## 🎯 Главное правило

**Все гипотезы о работе функциональности ОБЯЗАТЕЛЬНО проверяются через реальный фронтенд!**

Одна команда через фронтенд может разобрать проблему быстрее, чем множество предположений.











