# Чеклист для миграций с последовательностями PostgreSQL

## ⚠️ КРИТИЧЕСКИ ВАЖНО

При работе с миграциями, которые переименовывают таблицы или создают SERIAL поля, **ОБЯЗАТЕЛЬНО** проверять последовательности!

## 🚨 ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ ДЛЯ AI-АГЕНТА

**При создании или изменении миграций SQL AI-агент ОБЯЗАН:**

1. ✅ **Перед коммитом:** Запустить `scripts/validate-migration.ps1` для проверки миграции
2. ✅ **При создании миграции:** Использовать шаблон `backend/migrations/TEMPLATE_with_sequence.sql`
3. ✅ **После применения миграции:** Запустить `scripts/check-sequences-after-migration.ps1 -TableName <table_name>`
4. ✅ **При работе с репозиториями:** Использовать `BaseRepository` для автоматической обработки ошибок
5. ✅ **При коммите:** Pre-commit hook автоматически проверяет миграции

**Если хотя бы одна проверка не выполнена - задача НЕ считается завершенной!**

## Перед созданием миграции

- [ ] Определить, будет ли миграция переименовывать таблицы с SERIAL полями
- [ ] Определить, будет ли миграция создавать новые таблицы с SERIAL полями
- [ ] Изучить шаблон `backend/migrations/TEMPLATE_with_sequence.sql`

## При создании миграции

- [ ] Если переименовывается таблица - добавить переименование последовательности
- [ ] Если создается SERIAL поле - добавить выдачу прав доступа на последовательность
- [ ] Использовать шаблон `TEMPLATE_with_sequence.sql` как основу
- [ ] Проверить все возможные имена последовательностей (с суффиксами _seq, _seq1 и т.д.)

## Перед применением миграции

- [ ] Запустить валидацию:
  ```powershell
  .\scripts\validate-migration.ps1 -MigrationFile "backend\migrations\XXX_migration.sql"
  ```
- [ ] Протестировать на тестовой БД (если доступна)
- [ ] Убедиться, что миграция включает:
  - Переименование последовательности (если переименовывается таблица)
  - Выдачу прав доступа на последовательность

## После применения миграции

- [ ] Запустить проверку последовательностей:
  ```powershell
  .\scripts\check-sequences-after-migration.ps1 -TableName "table_name"
  ```
- [ ] Проверить имена последовательностей:
  ```powershell
  $env:PGPASSWORD="Jnvnszoe5971312059001"
  psql -U postgres -d b2bplatform -h localhost -p 5432 -c "SELECT sequence_name FROM information_schema.sequences WHERE sequence_name LIKE '%table_name%';"
  ```
- [ ] Проверить права доступа:
  ```powershell
  psql -U postgres -d b2bplatform -h localhost -p 5432 -c "\dp table_name_id_seq"
  ```
- [ ] Проверить, что приложение работает корректно
- [ ] Запустить тестовый парсинг для проверки сохранения данных

## Если обнаружена проблема

1. **Неправильное имя последовательности:**
   ```sql
   ALTER SEQUENCE wrong_name_id_seq RENAME TO correct_name_id_seq;
   ```

2. **Отсутствие прав доступа:**
   ```sql
   GRANT ALL PRIVILEGES ON SEQUENCE sequence_name TO postgres;
   GRANT ALL PRIVILEGES ON SEQUENCE sequence_name TO PUBLIC;
   ALTER SEQUENCE sequence_name OWNER TO postgres;
   ```

3. **Обновить миграцию** для будущих применений

4. **Задокументировать** в `docs/TROUBLESHOOTING.md`

## Примеры правильных миграций

См. `backend/migrations/004_fix_domains_queue_primary_key.sql` (обновленная версия)

## Связанные документы

- `docs/TROUBLESHOOTING.md` - Библия ошибок и решений
- `backend/migrations/TEMPLATE_with_sequence.sql` - Шаблон для миграций
- `scripts/validate-migration.ps1` - Скрипт валидации миграций
- `scripts/check-sequences-after-migration.ps1` - Скрипт проверки последовательностей

