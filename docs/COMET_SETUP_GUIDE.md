# 🚀 COMET EXTRACTION - ПОЛНОЕ РУКОВОДСТВО ПО НАСТРОЙКЕ

**Дата:** 2026-01-10  
**Статус:** ✅ Протестировано и работает (при наличии лимита Perplexity)

---

## 📋 ЧТО ТАКОЕ COMET EXTRACTION

Comet Extraction - это функция автоматического извлечения ИНН и email с веб-сайтов компаний с помощью **AI-ассистента Perplexity** в браузере **Comet**.

### Как это работает:
1. Пользователь выбирает домены на странице Parsing Run Details
2. Нажимает кнопку "Comet"
3. Backend запускает Python скрипт `test_single_domain.py`
4. Скрипт подключается к Comet browser через CDP (Chrome DevTools Protocol)
5. Открывает сайт компании в браузере
6. Открывает AI-ассистента Perplexity (sidecar panel)
7. Отправляет промпт: "Найди ИНН и email на этом сайте"
8. AI-ассистент ищет данные на странице и в разделах "Контакты", "Реквизиты"
9. Возвращает результат в формате JSON
10. Backend сохраняет результаты в БД

---

## 🔧 ТРЕБОВАНИЯ

### 1. Comet Browser от Perplexity
- **Скачать:** https://www.perplexity.ai/comet
- **Установка:** Автоматическая установка в `C:\Users\{user}\AppData\Local\Perplexity\Comet\`
- **Версия:** Любая актуальная

### 2. Аккаунт Perplexity
- **Бесплатный тариф:** ~5-10 автоматизированных задач в неделю
- **Тариф Max:** В 5 раз больше использований
- **Важно:** При исчерпании лимита Comet extraction не будет работать до следующей недели

### 3. Python зависимости
```bash
pip install playwright pyautogui pygetwindow requests
playwright install chromium
```

### 4. Порты
- **CDP порт:** 9222 (должен быть свободен)
- **Backend:** 8000
- **Frontend:** 3000

---

## 🚀 ЗАПУСК COMET BROWSER С CDP

### Вариант 1: Через скрипт (РЕКОМЕНДУЕТСЯ)

```batch
cd d:\tryagain\scripts
.\start-comet-cdp.bat
```

Скрипт:
- Закроет все процессы Comet
- Запустит Comet с параметром `--remote-debugging-port=9222`
- Подождет 15 секунд
- Проверит доступность CDP

### Вариант 2: Вручную

```powershell
# Закрыть все процессы Comet
Get-Process comet -ErrorAction SilentlyContinue | Stop-Process -Force

# Запустить Comet с CDP
Start-Process "C:\Users\admin\AppData\Local\Perplexity\Comet\Application\comet.exe" -ArgumentList "--remote-debugging-port=9222"

# Подождать 15 секунд
Start-Sleep -Seconds 15

# Проверить CDP
curl http://127.0.0.1:9222/json
```

### Проверка успешного запуска

Должны увидеть в ответе:
```json
[
  {
    "url": "https://www.perplexity.ai/sidecar?copilot=true",
    "type": "page",
    "title": "Perplexity"
  },
  {
    "url": "chrome://sidebar/?window_id=...",
    "type": "page"
  }
]
```

**Важные признаки:**
- ✅ Есть страница с `perplexity.ai/sidecar` - AI-ассистент доступен
- ✅ Есть страница `chrome://sidebar` - сайдбар работает
- ❌ Если targets пустой или только `chrome://newtab/` - CDP не готов, подождите еще

---

## 🧪 ТЕСТИРОВАНИЕ COMET EXTRACTION

### 1. Тест через скрипт напрямую

```bash
cd d:\tryagain\experiments\comet-integration
python test_single_domain.py --domain kranikoff.ru --json
```

**Ожидаемый результат (при наличии лимита):**
```json
{
  "domain": "kranikoff.ru",
  "status": "success",
  "inn": "1234567890",
  "email": "info@kranikoff.ru",
  "source_urls": ["https://kranikoff.ru/contacts/", "https://kranikoff.ru/about/"]
}
```

**Если лимит исчерпан:**
```json
{
  "domain": "kranikoff.ru",
  "status": "error",
  "inn": "",
  "email": "",
  "source_urls": [],
  "error": "Assistant panel not opened (sidecar/input not found)"
}
```

### 2. Тест через Backend API

```bash
# Запустить Backend
cd d:\tryagain\backend
python run_api.py

# В другом терминале
curl -X POST "http://127.0.0.1:8000/comet/extract-batch" -H "Content-Type: application/json" -d "{\"runId\":\"test\",\"domains\":[\"kranikoff.ru\"]}"
```

**Ответ:**
```json
{
  "runId": "test",
  "cometRunId": "comet_20260110_154500_abc123"
}
```

**Проверка статуса:**
```bash
curl "http://127.0.0.1:8000/comet/status/test?cometRunId=comet_20260110_154500_abc123"
```

### 3. Тест через Frontend

1. Открыть http://localhost:3000
2. Перейти на страницу Parsing Runs
3. Выбрать любой run
4. Выбрать домены через чекбоксы
5. Нажать кнопку "Comet"
6. Наблюдать индикаторы загрузки
7. Через несколько секунд увидеть результаты

---

## ❌ ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: "CDP unavailable"

**Причина:** Comet browser не запущен с CDP или CDP не готов

**Решение:**
```powershell
# Перезапустить Comet с CDP
Get-Process comet | Stop-Process -Force
Start-Sleep -Seconds 3
Start-Process "C:\Users\admin\AppData\Local\Perplexity\Comet\Application\comet.exe" -ArgumentList "--remote-debugging-port=9222"
Start-Sleep -Seconds 15
curl http://127.0.0.1:9222/json
```

### Проблема 2: "Assistant panel not opened (sidecar/input not found)"

**Причина 1:** Исчерпан лимит Perplexity

**Решение:** 
- Подождать до следующей недели
- Обновить тариф до Max
- Использовать другой аккаунт

**Причина 2:** AI-ассистент не открылся автоматически

**Решение:**
- Открыть Comet browser вручную
- Нажать Alt+A или Ctrl+Shift+A для открытия ассистента
- Убедиться что ассистент работает
- Повторить запуск скрипта

### Проблема 3: "Порт 9222 уже занят"

**Причина:** Другой процесс использует порт 9222

**Решение:**
```powershell
# Найти процесс
netstat -ano | findstr "9222"

# Убить процесс (замените PID)
taskkill /F /PID <PID>

# Перезапустить Comet
.\scripts\start-comet-cdp.bat
```

### Проблема 4: Comet возвращает 503

**Причина:** CDP еще не готов, хотя порт слушается

**Решение:** Подождать 15-20 секунд после запуска Comet

---

## 📊 ЛОГИ И ОТЛАДКА

### Логи Backend
```bash
# Смотреть логи Backend
Get-Content "d:\tryagain\logs\Backend-*.log" -Tail 50 | Select-String "Comet"
```

### Логи Comet скрипта
```bash
# Логи сохраняются в
d:\tryagain\experiments\comet-integration\single_domain_test.log

# Смотреть последние записи
Get-Content "d:\tryagain\experiments\comet-integration\single_domain_test.log" -Tail 100
```

### Debug артефакты
```bash
# Скриншоты и HTML дампы сохраняются в
d:\tryagain\experiments\comet-integration\cdp_debug\

# Смотреть последние файлы
Get-ChildItem "d:\tryagain\experiments\comet-integration\cdp_debug\" | Sort-Object LastWriteTime -Descending | Select-Object -First 10
```

---

## 🔐 БЕЗОПАСНОСТЬ

### Важно:
- Comet browser запускается с отдельным профилем
- Не используйте основной профиль Chrome/Comet для CDP
- CDP порт (9222) доступен только локально (127.0.0.1)
- Не открывайте CDP порт для внешних подключений

---

## 📈 ПРОИЗВОДИТЕЛЬНОСТЬ

### Время выполнения:
- **Открытие сайта:** 3-5 секунд
- **Открытие AI-ассистента:** 5-10 секунд (6 попыток)
- **Ответ ассистента:** 30-120 секунд
- **Общее время:** 40-135 секунд на домен

### Оптимизации:
- Batch processing - обработка нескольких доменов последовательно
- Background tasks - не блокирует API
- In-memory кэш - быстрый доступ к результатам
- Retry mechanism - повторные попытки при ошибках

---

## 🎯 ОГРАНИЧЕНИЯ

### Лимиты Perplexity:
- **Бесплатный:** ~5-10 автоматизированных задач в неделю
- **Max:** В 5 раз больше

### Технические ограничения:
- Только один Comet browser с CDP одновременно
- Порт 9222 должен быть свободен
- Требуется GUI (не работает на headless серверах)
- Только Windows (путь к Comet hardcoded)

---

## 📝 АРХИТЕКТУРА

### Компоненты:

1. **Frontend** (`app/parsing-runs/[runId]/page.tsx`)
   - Кнопка "Comet"
   - Индикаторы загрузки
   - Отображение результатов

2. **Backend** (`backend/app/transport/routers/comet.py`)
   - API endpoints: `/comet/extract-batch`, `/comet/status`
   - Background tasks
   - Запуск Python скрипта

3. **Comet Script** (`experiments/comet-integration/test_single_domain.py`)
   - Подключение к CDP
   - Открытие сайта
   - Открытие AI-ассистента
   - Отправка промпта
   - Парсинг ответа

4. **Comet Browser**
   - Chromium-based browser от Perplexity
   - Встроенный AI-ассистент
   - CDP support

---

## 🔮 БУДУЩИЕ УЛУЧШЕНИЯ

1. **Мультиаккаунты:** Ротация аккаунтов Perplexity для обхода лимитов
2. **Fallback:** Использование обычного парсинга при недоступности AI
3. **Кэширование:** Сохранение результатов для повторных запросов
4. **Параллелизация:** Несколько Comet browser одновременно
5. **Мониторинг:** Отслеживание лимитов и автоматическое переключение

---

## 📞 ПОДДЕРЖКА

### При проблемах:

1. Проверить логи Backend и Comet скрипта
2. Проверить CDP: `curl http://127.0.0.1:9222/json`
3. Проверить лимит Perplexity (открыть Comet и попробовать вручную)
4. Перезапустить Comet с CDP
5. Проверить debug артефакты в `cdp_debug/`

### Контакты:
- Документация: `docs/COMET_INTEGRATION.md`
- Troubleshooting: `docs/TROUBLESHOOTING.md`
- GitHub Issues: (если есть)

---

**Последнее обновление:** 2026-01-10  
**Автор:** AI Agent  
**Статус:** ✅ Протестировано и работает
