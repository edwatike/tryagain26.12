# Запуск сервера - Единое окно терминала

**⚠️ ВАЖНО: Это стандартный способ запуска всех сервисов проекта. Используйте его всегда!**

## 📋 Обзор

Все сервисы запускаются в **одном окне терминала** с:
- ✅ Цветным логированием (ошибки - красным, успех - зеленым)
- ✅ Мониторингом в реальном времени
- ✅ Автоматическими health checks
- ✅ Оптимизированной нагрузкой на систему

## 🚀 Быстрый запуск

### Вариант 1: Автоматический запуск (рекомендуется)

**Один клик - все сервисы запущены:**

```batch
start-all-tabby.bat
```

Это запустит:
1. Chrome CDP (если не запущен)
2. Parser Service (порт 9003)
3. Backend (порт 8000)
4. Frontend (порт 3000)
5. Монитор всех сервисов в одном окне

### Вариант 2: Ручной запуск через PowerShell

```powershell
powershell -ExecutionPolicy Bypass -File "scripts\start-all-services-single-window.ps1" -ProjectRoot "D:\tryagain" -Mode "debug"
```

## ⚙️ Режимы работы

### Debug Mode (по умолчанию)

**Характеристики:**
- Детальное логирование всех сообщений
- Быстрый мониторинг: каждые 2 секунды (startup) → 2.5 секунды (stable)
- Health checks: каждые 30 секунд (startup) → 60 секунд (stable)
- Показываются все логи: ошибки, предупреждения, информационные сообщения

**Когда использовать:**
- Разработка и отладка
- Диагностика проблем
- Первый запуск

**Запуск:**
```batch
start-all-tabby.bat
```
или
```powershell
powershell -ExecutionPolicy Bypass -File "scripts\start-all-services-single-window.ps1" -ProjectRoot "D:\tryagain" -Mode "debug"
```

### Production Mode (оптимизированный)

**Характеристики:**
- Минимальное логирование (только ошибки и предупреждения после стабилизации)
- Оптимизированный мониторинг: каждые 3.5 секунд (startup) → 5 секунд (stable)
- Health checks: каждые 70 секунд (startup) → 200 секунд (stable)
- Снижение нагрузки на систему на 60-80%

**Когда использовать:**
- Длительная работа сервера
- Продакшн-окружение
- Когда не нужны детальные логи

**Запуск:**
Отредактируйте `start-all-tabby.bat` и измените:
```batch
-Mode "debug"
```
на:
```batch
-Mode "production"
```

Или запустите напрямую:
```powershell
powershell -ExecutionPolicy Bypass -File "scripts\start-all-services-single-window.ps1" -ProjectRoot "D:\tryagain" -Mode "production"
```

## 📊 Что происходит при запуске

### Шаг 1: Остановка старых сервисов
Скрипт автоматически останавливает все запущенные сервисы (кроме Chrome CDP, если он уже работает в режиме отладки).

### Шаг 2: Запуск Chrome CDP
Chrome запускается в видимом режиме (не headless) с портом отладки 9222. Это необходимо для:
- Парсинга через Chrome CDP
- Возможности вручную пройти CAPTCHA

**Важно:** Chrome использует отдельный профиль отладки (`temp\chrome_debug_profile`), чтобы не конфликтовать с обычным Chrome.

### Шаг 3: Запуск всех сервисов
Сервисы запускаются в фоновом режиме:
- **Parser Service** → `http://127.0.0.1:9003`
- **Backend** → `http://127.0.0.1:8000`
- **Frontend** → `http://localhost:3000`

### Шаг 4: Мониторинг
В окне терминала отображаются:
- Логи всех сервисов с цветовой индикацией
- Health checks каждые 30-200 секунд (в зависимости от режима)
- Статус работы каждого сервиса

## 🎨 Цветовая индикация

- 🔴 **Красный** - Ошибки, критические проблемы
- 🟢 **Зеленый** - Успешные операции, запуск сервисов
- 🟡 **Желтый** - Предупреждения, уведомления
- ⚪ **Белый** - Информационные сообщения (только в debug mode)

## 📁 Логи

Логи каждого сервиса сохраняются в `logs/`:
- `Backend-YYYYMMDD-HHMMSS.log`
- `Parser Service-YYYYMMDD-HHMMSS.log`
- `Frontend-YYYYMMDD-HHMMSS.log`

**Автоматическая ротация:** Хранятся только последние 5 файлов на сервис.

## ✅ Проверка работоспособности

После запуска проверьте:

1. **Chrome CDP:**
   ```powershell
   Invoke-WebRequest http://127.0.0.1:9222/json/version
   ```

2. **Parser Service:**
   ```powershell
   Invoke-WebRequest http://127.0.0.1:9003/health
   ```

3. **Backend:**
   ```powershell
   Invoke-WebRequest http://127.0.0.1:8000/health
   ```

4. **Frontend:**
   ```powershell
   Invoke-WebRequest http://localhost:3000
   ```

Или откройте в браузере:
- Frontend: http://localhost:3000
- Backend API Docs: http://127.0.0.1:8000/docs
- Parser Service: http://127.0.0.1:9003

## 🛑 Остановка сервисов

Для остановки всех сервисов:
1. Нажмите `Ctrl+C` в окне монитора
2. Или используйте скрипт:
   ```powershell
   powershell -ExecutionPolicy Bypass -File "scripts\stop-all-services.ps1"
   ```

**Важно:** Chrome CDP не будет остановлен, если он был запущен вручную в режиме отладки.

## 🔧 Оптимизации производительности

### Что оптимизировано:

1. **Чтение логов:**
   - Используется `Get-Content -Tail` для чтения только новых строк
   - Вместо чтения всего файла каждые 2 секунды
   - Снижение нагрузки на диск и память в 10-100 раз

2. **Health checks:**
   - Частота проверок снижается после стабилизации сервисов
   - Debug: 30 сек → 60 сек
   - Production: 70 сек → 200 сек

3. **Логирование:**
   - В production mode после стабилизации показываются только ошибки и предупреждения
   - Информационные сообщения скрыты

4. **Ротация логов:**
   - Автоматическая очистка старых лог-файлов
   - Предотвращение бесконечного роста дискового пространства

### Результат:
- **Снижение нагрузки на систему на 60-80%**
- Минимальное использование CPU и памяти
- Стабильная работа при длительной эксплуатации

## 📝 Структура скриптов

```
scripts/
├── start-all-services-single-window.ps1  # Основной скрипт запуска
├── logger.ps1                            # Функции цветного логирования
├── stop-all-services.ps1                 # Остановка всех сервисов
└── chrome_config.bat                     # Конфигурация Chrome CDP

start-all-tabby.bat                       # Главный скрипт запуска (вызывает PowerShell скрипт)
```

## ⚠️ Важные замечания

1. **Chrome CDP:**
   - Должен быть запущен в видимом режиме (не headless)
   - Использует отдельный профиль отладки
   - Не закрывается автоматически при остановке других сервисов

2. **Порты:**
   - Убедитесь, что порты 8000, 9003, 3000, 9222 свободны
   - Если порт занят, скрипт покажет ошибку

3. **База данных:**
   - Убедитесь, что PostgreSQL запущен
   - База данных `b2bplatform` должна существовать
   - Миграции должны быть применены

4. **Режимы:**
   - Debug mode - для разработки и отладки
   - Production mode - для длительной работы

## 🔗 Связанные документы

- [`README.md`](../README.md) - Общее описание проекта
- [`docs/TROUBLESHOOTING.md`](TROUBLESHOOTING.md) - Решение проблем
- [`docs/PROJECT_SPECIFICATION.md`](PROJECT_SPECIFICATION.md) - Спецификация проекта

---

**Дата создания:** 2025-12-27  
**Последнее обновление:** 2025-12-27











