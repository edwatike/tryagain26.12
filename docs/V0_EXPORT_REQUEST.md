⚠️ **АРХИВ. Актуальная версия: см. `docs/MASTER_INSTRUCTION.md`**

# Запрос на экспорт информации о созданном фронтенде

## ⚠️ ВАЖНО: Нужна полная информация для интеграции

После генерации UI, пожалуйста, предоставь следующую информацию:

---

## 1. СТРУКТУРА ФАЙЛОВ

Предоставь полный список всех созданных/измененных файлов с их путями:

```
Пример:
- app/page.tsx (изменен)
- app/parsing-runs/page.tsx (новый)
- app/parsing-runs/[runId]/page.tsx (новый)
- components/ui/accordion.tsx (новый)
- lib/api.ts (изменен)
- lib/types.ts (изменен)
```

---

## 2. ПОЛНЫЙ КОД ВСЕХ ФАЙЛОВ

Предоставь полный код всех созданных и измененных файлов. Это критически важно для интеграции.

**Особенно важно:**
- Все страницы (app/**/*.tsx)
- Все компоненты (components/**/*.tsx)
- Изменения в lib/api.ts
- Изменения в lib/types.ts
- Новые типы TypeScript

---

## 3. ЗАВИСИМОСТИ (package.json)

Предоставь список всех новых зависимостей, которые нужно добавить:

```json
{
  "dependencies": {
    "новый-пакет": "версия"
  }
}
```

Или полный обновленный `package.json`, если были изменения.

---

## 4. КОНФИГУРАЦИОННЫЕ ФАЙЛЫ

Предоставь изменения в:
- `tailwind.config.ts` (если были изменения)
- `tsconfig.json` (если были изменения)
- `next.config.js` (если были изменения)
- Любые другие конфигурационные файлы

---

## 5. SHADCN UI КОМПОНЕНТЫ

Предоставь список всех Shadcn UI компонентов, которые были созданы:

```
Пример:
- components/ui/accordion.tsx
- components/ui/dialog.tsx
- components/ui/alert-dialog.tsx
```

И их полный код, если они были созданы.

---

## 6. API ИНТЕГРАЦИИ

Предоставь информацию о:
- Какие новые функции были добавлены в `lib/api.ts`
- Какие новые типы были добавлены в `lib/types.ts`
- Как используются API endpoints

---

## 7. НАВИГАЦИЯ И РОУТИНГ

Предоставь информацию о:
- Какие новые страницы созданы
- Какие маршруты используются
- Как работает навигация между страницами

---

## 8. СОСТОЯНИЕ И УПРАВЛЕНИЕ ДАННЫМИ

Предоставь информацию о:
- Какие хуки используются (useState, useEffect, useRouter)
- Как загружаются данные
- Как обрабатываются ошибки
- Как показываются loading states

---

## 9. СТИЛИ И ТЕМЫ

Предоставь информацию о:
- Какие Tailwind классы используются
- Какие кастомные стили добавлены
- Изменения в `globals.css` (если были)

---

## 10. ACCORDION РЕАЛИЗАЦИЯ (КРИТИЧЕСКИ ВАЖНО)

Предоставь полный код компонента accordion для результатов парсинга:
- Как реализована группировка по доменам
- Как работает фильтрация blacklist
- Как реализованы действия для доменов (Add to Blacklist, Create Supplier, Create Reseller)
- Функция `extractRootDomain()` (если создана)
- Функция `groupByDomain()` (если создана)

---

## 11. МОДАЛЬНЫЕ ОКНА И ДИАЛОГИ

Предоставь код всех модальных окон:
- Модал для создания поставщика
- Модал для создания реселлера
- Модал для добавления в blacklist (если есть)
- AlertDialog для подтверждения удаления

---

## 12. ОБРАБОТКА ОШИБОК

Предоставь информацию о:
- Как обрабатываются API ошибки
- Какие toast уведомления используются
- Как показываются ошибки пользователю

---

## 13. ПРОВЕРКА РАБОТОСПОСОБНОСТИ

После предоставления всей информации, укажи:
- Какие страницы были протестированы
- Какие функции работают
- Какие функции требуют дополнительной настройки
- Известные проблемы или ограничения

---

## ФОРМАТ ОТВЕТА

Пожалуйста, предоставь информацию в следующем формате:

```markdown
# Экспорт фронтенда от V0

## 1. Структура файлов
[список файлов]

## 2. Код файлов
### app/page.tsx
```typescript
[полный код]
```

### app/parsing-runs/[runId]/page.tsx
```typescript
[полный код]
```
[... и так далее для всех файлов]

## 3. Зависимости
[package.json или список зависимостей]

## 4. Конфигурация
[изменения в конфигурационных файлах]

## 5. Shadcn UI компоненты
[список и код компонентов]

## 6. API интеграции
[информация об API]

## 7. Навигация
[информация о роутинге]

## 8. Состояние
[информация о state management]

## 9. Стили
[информация о стилях]

## 10. Accordion реализация
[полный код accordion компонента]

## 11. Модальные окна
[код всех модалов]

## 12. Обработка ошибок
[информация об обработке ошибок]

## 13. Проверка работоспособности
[статус проверки]
```

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**Предоставь ВСЮ информацию сразу, чтобы я мог:**
1. Правильно интегрировать код в существующий проект
2. Сохранить все существующие функции
3. Не сломать текущую работу
4. Правильно настроить все зависимости
5. Проверить совместимость с существующим кодом

**Без полной информации интеграция будет неполной или может сломать существующий код!**

---

## ЧТО НУЖНО ОСОБЕННО ВНИМАТЕЛЬНО

1. **Accordion компонент** - должен быть полностью рабочим с группировкой по доменам
2. **API функции** - должны использовать существующий `apiFetch` из `lib/api.ts`
3. **Типы TypeScript** - должны быть совместимы с существующими типами
4. **Навигация** - должна использовать `useRouter` из `next/navigation`
5. **Стили** - должны использовать Tailwind CSS и Shadcn UI компоненты

---

**СПАСИБО! Эта информация критически важна для успешной интеграции.**

