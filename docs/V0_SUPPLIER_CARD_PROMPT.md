⚠️ **АРХИВ. Актуальная версия: см. `docs/MASTER_INSTRUCTION.md`**

# Промт для V0: Карточка поставщика с данными Checko

## Контекст проекта

B2B-платформа для модерации поставщиков. Backend на FastAPI, Frontend на Next.js 16 с TypeScript, Tailwind CSS, Shadcn UI. Интеграция с Checko API для получения данных о компаниях.

## Структура данных из Backend API

### Endpoint: `GET /moderator/suppliers/{id}`

**Response (SupplierDTO):**
```typescript
{
  id: number
  name: string                    // Название компании
  inn: string | null              // ИНН (10 или 12 цифр)
  email: string | null
  domain: string | null
  address: string | null
  type: "supplier" | "reseller"   // Тип: поставщик или реселлер
  
  // Checko поля (из API)
  ogrn?: string | null            // ОГРН
  kpp?: string | null             // КПП
  okpo?: string | null            // ОКПО
  companyStatus?: string | null    // Статус компании (например, "Действующая")
  registrationDate?: string | null // Дата регистрации (ISO format)
  legalAddress?: string | null    // Юридический адрес
  phone?: string | null           // Телефон
  website?: string | null         // Веб-сайт
  vk?: string | null              // ВКонтакте
  telegram?: string | null         // Telegram
  authorizedCapital?: number | null // Уставной капитал (в рублях)
  
  // Финансовые данные (последний год)
  revenue?: number | null         // Выручка (код 2110, в рублях)
  profit?: number | null           // Прибыль (код 2400, в рублях)
  financeYear?: number | null     // Год финансовых данных
  
  // Судебные дела
  legalCasesCount?: number | null        // Всего дел
  legalCasesSum?: number | null           // Общая сумма исков (в рублях)
  legalCasesAsPlaintiff?: number | null  // Как истец
  legalCasesAsDefendant?: number | null  // Как ответчик
  
  checkoData?: string | null      // Полный JSON с данными Checko (для детального отображения)
  
  createdAt: string
  updatedAt: string
}
```

### Дополнительные данные из checkoData (JSON)

Если `checkoData` присутствует, можно распарсить JSON для получения:
- `_finances`: объект с финансовыми данными по годам `{ "2024": {...}, "2023": {...} }`
- `_legal`: данные о судебных делах
- `_inspections`: данные о проверках
- `_enforcements`: данные об исполнительных производствах
- `Учред`: массив учредителей
- `Руковод`: массив руководителей

## Структура карточки (5 секций)

### 1. HEADER (верхняя строка)
```
[Название компании] [Бейдж: Поставщик/Реселлер] [⭐ Рейтинг Checko если есть]

Адрес | Телефон | Сайт | ВК | Telegram
```

**Детали:**
- Название: `name` (крупный шрифт, bold)
- Бейдж типа: зеленый для "Поставщик", фиолетовый для "Реселлер"
- Рейтинг Checko: если есть в checkoData, показать (например, "+553")
- Метаданные: `legalAddress`, `phone`, `website`, `vk`, `telegram` (иконки, ссылки)

### 2. ФИНАНСЫ (гармошка по годам)
```
Выручка: 147.5 млрд (+18%) | Прибыль: 9.7 млрд (+4%)

▼ 2024 | 2023 | 2022 | 2021
[График или таблица по годам]
```

**Детали:**
- Текущий год: `financeYear`, `revenue`, `profit`
- Расчет изменения: сравнить с предыдущим годом (если есть в `_finances`)
- Accordion с годами: раскрывающийся список с данными по каждому году
- График: line chart выручки и прибыли (4 года, если доступно)
- Форматирование: миллиарды/миллионы с суффиксами (147.5 млрд, 9.7 млрд)

### 3. РИСКИ (цветовая индикация)
```
🟢 Арбитраж: Истец 1063 | Ответчик 73
🟡 Проверки: 372 (20 нарушений) [если есть в checkoData]
🔴 Исп.производства: 1 долг [если есть в checkoData]
```

**Детали:**
- Арбитраж: `legalCasesAsPlaintiff` / `legalCasesAsDefendant`
  - Зеленый если истец > ответчик (хорошо)
  - Желтый если примерно равны
  - Красный если ответчик >> истец (плохо)
- Проверки: из `_inspections` (если есть)
- Исполнительные производства: из `_enforcements` (если есть)
- Цветовая индикация: эмодзи + цвет фона

### 4. УЧРЕДИТЕЛИ (список)
```
👤 Иванов И.И. (50%)
👤 Петров П.П. (25%)
👤 Сидоров С.С. (25%)
```

**Детали:**
- Данные из `checkoData.Учред` (массив)
- Формат: ФИО + доля (если есть)
- Иконка пользователя для каждого

### 5. ДЕЙСТВИЯ (кнопки снизу)
```
[✏️ Редактировать] [🚫 Blacklist] [📋 Ключевые слова]
```

**Детали:**
- Редактировать: ссылка на `/suppliers/{id}/edit`
- Blacklist: действие добавления в черный список
- Ключевые слова: ссылка на список ключевых слов поставщика

## Стиль V0/Figma 2025

### Цветовая палитра
- **Primary**: `#3B82F6` (синий)
- **Success**: `#10B981` (зеленый)
- **Warning**: `#F59E0B` (желтый/оранжевый)
- **Danger**: `#EF4444` (красный)
- **Background**: белый/светло-серый
- **Text**: темно-серый/черный

### Эстетика
- **Linear/Notion стиль**: минималистичный, чистый дизайн
- **Карточки**: белый фон, тень `shadow-lg`, скругленные углы `rounded-lg`
- **Типографика**: 
  - Заголовки: `font-bold`, `text-2xl` / `text-3xl`
  - Текст: `text-sm` / `text-base`
  - Метаданные: `text-xs`, `text-muted-foreground`

### Анимации
- **Accordion**: плавное раскрытие/закрытие (`transition-all`, `duration-300`)
- **Hover эффекты**: `hover:shadow-lg`, `hover:scale-105` для кнопок
- **График**: плавная анимация появления данных

### Компоненты Shadcn UI
- `Card`, `CardHeader`, `CardTitle`, `CardContent`
- `Badge` (для типов, статусов)
- `Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent`
- `Button` (варианты: default, outline, ghost)
- `Separator` (для разделения секций)

## Сценарии использования

### 1. Загрузка (skeleton)
- Показывать skeleton loader во время загрузки данных
- Анимированные placeholder'ы для всех секций

### 2. Пустые данные
- Если `checkoData` отсутствует: показать сообщение "Данные Checko недоступны"
- Кнопка "Загрузить из Checko" (если есть ИНН)

### 3. Риски (цветовая индикация)
- Зеленый: низкие риски (истец > ответчик, нет исполнительных производств)
- Желтый: средние риски (примерно равные истец/ответчик, есть проверки)
- Красный: высокие риски (ответчик >> истец, есть исполнительные производства)

### 4. Мобильная версия
- Адаптивная верстка: на мобильных секции располагаются вертикально
- График: упрощенная версия или таблица на мобильных
- Кнопки: full-width на мобильных

## Интеграция с Backend

### API Endpoints
- `GET /moderator/suppliers/{id}` - получение данных поставщика
- `GET /moderator/suppliers/{id}/keywords` - получение ключевых слов
- `GET /moderator/checko/{inn}` - получение данных Checko (с кэшированием)

### Типы TypeScript
```typescript
// lib/types.ts
export interface SupplierDTO {
  // ... (см. выше)
}

export interface SupplierKeyword {
  keyword: string
  urlCount: number
}
```

### Форматирование данных
- **Валюта**: функция `formatCurrency(value)` - форматирует рубли (147.5 млрд)
- **Даты**: функция `formatDate(dateString)` - форматирует даты в русский формат
- **Проценты**: расчет изменения между годами: `((new - old) / old) * 100`

## Пример реальных данных

```json
{
  "id": 1,
  "name": "ООО \"СТД ПЕТРОВИЧ\"",
  "inn": "7802348846",
  "type": "supplier",
  "revenue": 147500000000,
  "profit": 9700000000,
  "financeYear": 2024,
  "legalCasesAsPlaintiff": 1063,
  "legalCasesAsDefendant": 73,
  "legalAddress": "г. Санкт-Петербург, ул. Примерная, д. 1",
  "phone": "+7 (812) 123-45-67",
  "website": "https://example.com"
}
```

## Технические требования

- **Next.js 16** с App Router
- **TypeScript** строгая типизация
- **Tailwind CSS** для стилей
- **Shadcn UI** компоненты
- **Responsive design** (mobile-first)
- **Accessibility** (ARIA labels, keyboard navigation)

## Ожидаемый результат

Красивая, информативная карточка поставщика с:
- Четкой структурой (5 секций)
- Визуализацией финансов (график)
- Цветовой индикацией рисков
- Плавными анимациями
- Адаптивным дизайном
- Интеграцией с существующим API

## Путь к файлам проекта

**Рабочая директория:** `D:\tryagain\frontend\supplier-moderation-dashboard`

**Структура файлов:**
- `app/page.tsx` - главная страница (можно использовать для демонстрации)
- `components/supplier-card.tsx` - компонент карточки поставщика (уже существует, можно улучшить)
- `lib/types.ts` - типы данных (уже существует)
- `lib/format-utils.ts` - утилиты форматирования (уже существует)

**Для создания страницы детального просмотра:**
- `app/suppliers/[id]/page.tsx` - страница детального просмотра поставщика
- Или улучшить существующий `components/supplier-card.tsx`

---

**Используй этот промт в V0/Figma AI для генерации/улучшения компонента в `D:\tryagain\frontend\supplier-moderation-dashboard`**

**Важно:** 
- Проект уже имеет базовую структуру с `SupplierCard` компонентом
- Можно улучшить существующий компонент или создать новую страницу
- Все типы данных уже определены в `lib/types.ts`
- Утилиты форматирования уже есть в `lib/format-utils.ts`

