# Рекомендации по архитектуре проекта

**⚠️ ВАЖНО: Этот документ содержит рекомендации для улучшения архитектуры проекта. Рекомендации должны быть проверены на корректность и соответствие структуре проекта перед применением.**

**📚 Связанные документы:**
- **Спецификация**: [`docs/PROJECT_SPECIFICATION.md`](PROJECT_SPECIFICATION.md)
- **Правила работы**: [`.cursorrules`](../../.cursorrules)
- **Библия ошибок**: [`docs/TROUBLESHOOTING.md`](TROUBLESHOOTING.md)

---

## 1. Организация временных файлов

### Текущая структура

```
temp/
├── backend/
│   └── check_imports.py          # ✅ Скрипт проверки импортов
└── DEBUG_SUPPLIERS_ENDPOINT.md   # ⚠️ Временный файл отладки
```

### Рекомендации

**✅ ХОРОШО:**
- Скрипты проверки в `temp/backend/`
- Временные файлы отладки в `temp/`

**❌ ПЛОХО:**
- Временные файлы в корне проекта
- Тестовые файлы в основных директориях
- Скрипты без четкого назначения

**Правила:**
1. Все временные файлы должны быть в `temp/` или `experiments/`
2. После завершения работы - удалять или перемещать в архив
3. Не засорять структуру проекта

---

## 2. Структура Backend

### Текущая структура

```
backend/app/
├── adapters/          # ✅ Внешние сервисы
├── domain/           # ✅ Доменная логика
├── transport/        # ✅ HTTP слой
├── usecases/         # ✅ Бизнес-логика
├── config.py         # ✅ Конфигурация
└── main.py           # ✅ Точка входа
```

### Рекомендации

**✅ ХОРОШО:**
- Четкое разделение слоев
- Роутеры только для маршрутизации
- Usecases для бизнес-логики
- Adapters для внешних систем

**Рекомендации для улучшения:**

1. **Создать общий модуль для обработки ошибок:**
   ```
   backend/app/
   └── core/
       ├── __init__.py
       ├── exceptions.py      # Кастомные исключения
       └── error_handler.py  # Обработка ошибок
   ```

2. **Создать общий модуль для валидации:**
   ```
   backend/app/
   └── core/
       └── validators.py      # Общие валидаторы
   ```

3. **Создать общий модуль для утилит:**
   ```
   backend/app/
   └── core/
       └── utils.py           # Общие утилиты
   ```

**Правила:**
- Новые модули создавать только при необходимости
- Не создавать модули "на будущее"
- Следовать принципу единственной ответственности

---

## 3. Структура Frontend

### Текущая структура

```
frontend/moderator-dashboard-ui/
├── app/              # ✅ Next.js App Router
├── components/       # ✅ React компоненты
├── lib/              # ✅ Утилиты и типы
└── ...
```

### Рекомендации

**✅ ХОРОШО:**
- Использование App Router
- Разделение компонентов и страниц
- Централизованные типы в `lib/types.ts`

**Рекомендации для улучшения:**

1. **Создать общий модуль для обработки ошибок:**
   ```
   frontend/moderator-dashboard-ui/
   └── lib/
       └── error-handler.ts   # Обработка ошибок
   ```

2. **Создать общий компонент для отображения ошибок:**
   ```
   frontend/moderator-dashboard-ui/
   └── components/
       └── error-display.tsx  # Компонент ошибок
   ```

3. **Создать хуки для переиспользования логики:**
   ```
   frontend/moderator-dashboard-ui/
   └── hooks/
       ├── use-supplier.ts    # Хук для работы с поставщиками
       └── use-keywords.ts    # Хук для работы с keywords
   ```

**Правила:**
- Создавать хуки только при повторном использовании логики
- Не создавать хуки "на будущее"
- Следовать принципу DRY (Don't Repeat Yourself)

---

## 4. Документация

### Текущая структура

```
docs/
├── DOCUMENTATION_INDEX.md      # ✅ Индекс документации
├── TROUBLESHOOTING.md          # ✅ Библия ошибок
├── PROJECT_SPECIFICATION.md    # ✅ Спецификация
├── DIAGNOSTICS_PROCESS.md      # ✅ Процесс диагностики
└── README.md                   # ✅ Описание документации
```

### Рекомендации

**✅ ХОРОШО:**
- Четкая структура документации
- Индекс для навигации
- Отдельный документ для ошибок

**Рекомендации для улучшения:**

1. **Создать документ с примерами использования:**
   ```
   docs/
   └── EXAMPLES.md              # Примеры использования API
   ```

2. **Создать документ с архитектурными решениями:**
   ```
   docs/
   └── ARCHITECTURE_DECISIONS.md # Архитектурные решения
   ```

**Правила:**
- Документация должна быть актуальной
- Документация должна обновляться при изменении кода
- Документация должна быть понятной

---

## 5. Тестирование

### Текущая структура

```
backend/tests/
├── unit/              # ✅ Unit тесты
├── integration/       # ✅ Integration тесты
└── contract/          # ✅ Contract тесты
```

### Рекомендации

**✅ ХОРОШО:**
- Разделение типов тестов
- Использование pytest

**Рекомендации для улучшения:**

1. **Добавить тесты для Frontend:**
   ```
   frontend/moderator-dashboard-ui/
   └── __tests__/
       ├── components/  # Тесты компонентов
       └── lib/        # Тесты утилит
   ```

2. **Добавить E2E тесты:**
   ```
   tests/
   └── e2e/            # E2E тесты
   ```

**Правила:**
- Тесты должны быть быстрыми
- Тесты должны быть изолированными
- Тесты должны быть понятными

---

## 6. Автоматизация

### Рекомендации

1. **Pre-commit hooks:**
   ```bash
   # .git/hooks/pre-commit
   #!/bin/sh
   python temp/backend/check_imports.py
   exit $?
   ```

2. **CI/CD скрипты:**
   ```
   .github/workflows/
   └── ci.yml           # CI/CD pipeline
   ```

3. **Скрипты для разработки:**
   ```
   scripts/
   ├── check-all.sh    # Проверка всего проекта
   └── setup-dev.sh    # Настройка окружения
   ```

**Правила:**
- Скрипты должны быть в `scripts/` или `temp/`
- Скрипты должны быть документированы
- Скрипты должны быть проверены на работоспособность

---

## 7. Приоритеты внедрения

### Высокий приоритет

1. ✅ **Создать общий модуль обработки ошибок** (Backend и Frontend)
2. ✅ **Добавить pre-commit hook для проверки импортов**
3. ✅ **Создать общий компонент для отображения ошибок** (Frontend)

### Средний приоритет

4. ⚠️ **Создать хуки для переиспользования логики** (Frontend)
5. ⚠️ **Добавить тесты для Frontend**
6. ⚠️ **Создать документ с примерами использования**

### Низкий приоритет

7. 📝 **Создать документ с архитектурными решениями**
8. 📝 **Добавить E2E тесты**
9. 📝 **Создать CI/CD pipeline**

---

## 8. Правила внедрения рекомендаций

**Перед внедрением рекомендации:**

1. ✅ Проверить, что рекомендация не засоряет структуру проекта
2. ✅ Проверить, что рекомендация соответствует архитектуре
3. ✅ Проверить, что рекомендация решает реальную проблему
4. ✅ Проверить, что рекомендация не дублирует существующий функционал

**После внедрения рекомендации:**

1. ✅ Обновить документацию
2. ✅ Проверить работоспособность
3. ✅ Обновить спецификацию (если применимо)

---

## 9. История изменений

- **2025-12-26**: Создан документ с рекомендациями по архитектуре проекта

---

**Помни: Рекомендации должны улучшать проект, а не засорять его. Проверяй перед внедрением!**



















