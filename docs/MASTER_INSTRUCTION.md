# B2B Platform - Главная инструкция

**⚠️ ВАЖНО: Это главный документ проекта. Начните отсюда!**

Этот документ содержит всю необходимую информацию для запуска, настройки и работы с проектом B2B Platform.

## 📚 Навигация по документации

- **Этот файл** (`MASTER_INSTRUCTION.md`) - главная точка входа, общая информация, запуск и настройка
- **`TROUBLESHOOTING.md`** - библия ошибок и решений (начинайте отсюда при диагностике проблем)
- **`CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md`** - критические точки интеграции и чеклисты для изменений
- **`PROJECT_SPECIFICATION.md`** - детальная спецификация API и рабочих связок Backend-Frontend
- **`PROJECT_MAP.md`** - полная карта структуры проекта
- **`COMET_INTEGRATION.md`** - документация по Comet AI-ассистенту для извлечения ИНН и email

---

## 🚀 Для нового разработчика

Если вы впервые работаете с этим проектом, выполните следующие шаги по порядку:

### Шаг 1: Установите зависимости

1. **Python 3.12+**
   - Проверка: `python --version`
   - Скачать: https://www.python.org/downloads/

2. **Node.js 18+**
   - Проверка: `node --version`
   - Скачать: https://nodejs.org/

3. **PostgreSQL** (любая версия)
   - Проверка: `psql --version`
   - Скачать: https://www.postgresql.org/download/
   - Убедитесь, что PostgreSQL запущен и доступен

4. **Google Chrome**
   - Необходим для парсинга через Chrome CDP
   - Проверка: наличие Chrome в системе

### Шаг 2: Подготовьте .env файлы

Создайте файлы с переменными окружения (см. раздел 3.3 для деталей):

- `backend/.env` - настройки Backend (DATABASE_URL, PARSER_SERVICE_URL и др.)
- `frontend/moderator-dashboard-ui/.env.local` - настройки Frontend (NEXT_PUBLIC_API_URL, NEXT_PUBLIC_CHECKO_API_KEY)
- `parser_service/.env` - настройки Parser Service (CHROME_CDP_URL)

**Важно:** Используйте переменные окружения, не жёстко вписывайте пароли и URL в код или документацию.

### Шаг 3: Настройте базу данных

1. Создайте базу данных PostgreSQL (или используйте существующую)
2. Примените миграции (см. раздел 3.2)
3. Проверьте подключение к БД

### Шаг 4: Запустите все сервисы

**Рекомендуемый способ (один клик):**

```batch
start-all-tabby.bat
```

Или через PowerShell:

```powershell
powershell -ExecutionPolicy Bypass -File "scripts\start-all-services-single-window.ps1" -ProjectRoot "D:\tryagain" -Mode "debug"
```

Это запустит все сервисы в одном окне терминала:
- Chrome CDP (порт 9222)
- Parser Service (порт 9003)
- Backend (порт 8000)
- Frontend (порт 3000)

**Откройте:** http://localhost:3000

### Шаг 5: Выполните проверку работоспособности

```powershell
.\scripts\quick-check.ps1 all
```

Это проверит:
- Импорты Backend
- Линтер Frontend
- Health endpoints всех сервисов

### Шаг 6: При проблемах

Если что-то не работает:

1. **Сначала** выполните проверки из `docs/TROUBLESHOOTING.md` раздел "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ"
2. **Затем** используйте чеклист из `docs/TROUBLESHOOTING.md` раздел "Чеклист диагностики"
3. **Ищите** похожие проблемы в `docs/TROUBLESHOOTING.md`
4. Если решения нет - решите проблему и задокументируйте её в `docs/TROUBLESHOOTING.md`

---

## 1. Описание проекта

B2B Platform - система для автоматизации поиска, парсинга и модерации поставщиков с интеграцией Checko API.

### Назначение

- Автоматический поиск поставщиков через Google и Yandex
- Парсинг сайтов поставщиков через Chrome CDP
- Модерация и управление поставщиками
- Интеграция с Checko API для получения данных о компаниях

### Технологический стек

- **Frontend**: Next.js 16 (App Router), TypeScript, Tailwind CSS, Shadcn UI
- **Backend**: FastAPI (Python), SQLAlchemy 2.0, PostgreSQL
- **Parser Service**: FastAPI, Playwright, Chrome CDP
- **База данных**: PostgreSQL 15 (база `b2bplatform`)

---

## 2. Архитектура системы

### 2.1 Общая структура

Система состоит из четырех независимых компонентов:

1. **Backend (FastAPI)** - API сервер и бизнес-логика
   - **Роль:** Обработка HTTP запросов, бизнес-логика, работа с БД, координация между сервисами
   - **Порт:** `8000`
   - **Базовый URL:** `http://127.0.0.1:8000`
   - **Документация API:** `http://127.0.0.1:8000/docs`
   - **Общение:** HTTP с Frontend и Parser Service, PostgreSQL для БД

2. **Frontend (Next.js)** - Пользовательский интерфейс
   - **Роль:** Отображение данных, взаимодействие с пользователем, отправка запросов к Backend
   - **Порт:** `3000`
   - **URL:** `http://localhost:3000`
   - **Общение:** HTTP с Backend (через `lib/api.ts`)

3. **Parser Service** - Сервис парсинга через Chrome CDP
   - **Роль:** Поиск поставщиков через Google/Yandex, парсинг HTML, извлечение данных
   - **Порт:** `9003`
   - **Базовый URL:** `http://127.0.0.1:9003`
   - **Общение:** HTTP с Backend, Chrome CDP для парсинга

4. **Chrome CDP** - Chrome в режиме отладки
   - **Роль:** Выполнение JavaScript, рендеринг страниц, взаимодействие с поисковыми системами
   - **Порт:** `9222`
   - **URL:** `http://127.0.0.1:9222`
   - **Общение:** Chrome DevTools Protocol с Parser Service

### 2.1.1 Потоки данных

**Основные потоки:**

1. **Поиск и парсинг поставщиков:**
   - Frontend → Backend (`POST /parsing/start`) → создание записи в БД (parsing_request, parsing_run)
   - Backend → Parser Service (`POST /parse`) → запуск парсинга
   - Parser Service → Chrome CDP → поиск в Google/Yandex → извлечение ссылок
   - Parser Service → Backend → сохранение доменов в `domains_queue`
   - Backend → БД → обновление статуса парсинга
   - Frontend → Backend (`GET /parsing/runs/{run_id}`) → отображение результатов

2. **Модерация поставщиков:**
   - Frontend → Backend (`GET /moderator/suppliers`) → получение списка поставщиков
   - Frontend → Backend (`PUT /moderator/suppliers/{id}`) → обновление данных поставщика
   - Backend → БД → сохранение изменений
   - Backend → БД → запись в audit_log (аудит изменений)

3. **Интеграция с Checko:**
   - Frontend → Backend (`GET /moderator/checko/{inn}`) → запрос данных о компании
   - Backend → Checko API → получение данных о компании (финансы, судебные дела и т.д.)
   - Backend → БД → сохранение данных в `moderator_suppliers.checko_data` (сжатый JSON)
   - Backend → Frontend → отображение данных Checko

4. **Управление черным списком:**
   - Frontend → Backend (`POST /moderator/blacklist`) → добавление домена в blacklist
   - Backend → БД → сохранение в таблицу `blacklist`
   - При парсинге: Backend фильтрует домены через blacklist перед сохранением в `domains_queue`

### 2.2 Структура проекта

```
tryagain/
├── backend/              # Backend сервис (FastAPI)
│   ├── app/
│   │   ├── transport/   # HTTP слой (роутеры, схемы)
│   │   ├── usecases/    # Бизнес-логика
│   │   ├── adapters/    # Внешние сервисы (Parser, Checko, БД)
│   │   └── db/          # Работа с БД (модели, репозитории)
│   ├── migrations/      # SQL миграции БД
│   └── requirements.txt
│
├── frontend/
│   └── moderator-dashboard-ui/  # Next.js Frontend
│       ├── app/         # App Router страницы
│       ├── components/  # React компоненты
│       └── lib/          # Утилиты и API клиент
│
├── parser_service/       # Parser Service
│   ├── src/             # Основной код
│   └── api.py           # FastAPI приложение
│
├── docs/                 # Документация
├── scripts/              # Скрипты автоматизации
├── logs/                 # Логи сервисов
└── temp/                 # Временные файлы и эксперименты
    ├── backend/         # Временные скрипты для Backend
    └── experiments/     # Экспериментальные версии и дизайн-проекты
        ├── moderator-dashboardNEW/        # Старая версия frontend
        ├── supplier-card-design/          # Дизайн-проект карточки поставщика
        └── supplier-moderation-dashboard/ # Старая версия dashboard
```

### 2.3 Слои Backend

```
backend/app/
├── transport/          # HTTP слой (роутеры, схемы)
│   ├── routers/       # FastAPI endpoints (ТОЛЬКО маршрутизация)
│   └── schemas/       # Pydantic схемы (DTO)
├── usecases/          # Бизнес-логика (чистая логика без HTTP)
├── adapters/          # Внешние сервисы (Parser, Checko, БД)
│   ├── db/           # Работа с БД (модели, репозитории)
│   └── parser_client.py
└── domain/            # Доменная логика (если требуется)
```

**Правила:**
- Роутеры (`transport/routers`) - ТОЛЬКО маршрутизация, НЕ бизнес-логика
- Usecases - вся бизнес-логика, независимая от HTTP
- Adapters - взаимодействие с внешними системами
- Запрещено: писать SQL или сложную логику прямо в роутерах

### 2.4 Порты и сервисы

- **Backend:** `8000` (http://127.0.0.1:8000)
- **Frontend:** `3000` (http://localhost:3000)
- **Parser Service:** `9003` (http://127.0.0.1:9003)
- **Chrome CDP:** `9222` (http://127.0.0.1:9222)

**Health endpoints:**
- Backend: `GET http://127.0.0.1:8000/health`
- Parser Service: `GET http://127.0.0.1:9003/health`
- Chrome CDP: `GET http://127.0.0.1:9222/json/version`

---

## 3. Быстрый старт

### 3.1 Требования

- Python 3.12+
- Node.js 18+
- PostgreSQL (любая версия, используется база `b2bplatform`)
- Google Chrome (для парсинга)

### 3.2 Настройка базы данных

**⚠️ ВАЖНО: Используйте переменные окружения для подключения к БД!**

Настройте подключение к PostgreSQL через переменные окружения:

- **DATABASE_URL** - полная строка подключения (формат: `postgresql+asyncpg://user:password@host:port/database`)
- **PGPASSWORD** - пароль для PostgreSQL (используется в командах psql)

**Пример DATABASE_URL:**
```
postgresql+asyncpg://user:password@localhost:5432/database_name
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:**
- Не храните пароли в коде или документации
- Используйте переменные окружения или `.env` файлы
- Для production используйте безопасное хранение секретов

#### Проверка базы данных

```powershell
# Установите переменную окружения PGPASSWORD
$env:PGPASSWORD="your_postgres_password"

# Проверьте существование базы данных
psql -U postgres -h localhost -p 5432 -c "\l" | Select-String "your_database_name"
```

Если базы нет, создайте её:

```powershell
$env:PGPASSWORD="your_postgres_password"
psql -U postgres -h localhost -p 5432 -c "CREATE DATABASE your_database_name;"
```

#### Применение миграций

```powershell
# Установите переменную окружения PGPASSWORD
$env:PGPASSWORD="your_postgres_password"

# Примените миграции
psql -U postgres -d your_database_name -h localhost -p 5432 -f backend\migrations\001_initial_schema.sql
psql -U postgres -d your_database_name -h localhost -p 5432 -f backend\migrations\002_audit_log.sql
psql -U postgres -d your_database_name -h localhost -p 5432 -f backend\migrations\003_parsing_requests.sql
psql -U postgres -d your_database_name -h localhost -p 5432 -f backend\migrations\004_fix_domains_queue_primary_key.sql
```

**Примечание:** Замените `your_postgres_password`, `your_database_name` на ваши реальные значения из переменных окружения.

### 3.3 Создание .env файлов

#### Backend (`backend/.env`)

```env
DATABASE_URL=postgresql+asyncpg://user:password@host:port/database_name
PARSER_SERVICE_URL=http://127.0.0.1:9003
ENV=development
LOG_LEVEL=INFO
LOG_SQL=false
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
ATTACHMENTS_DIR=storage/attachments
```

**Описание переменных:**
- `DATABASE_URL` - строка подключения к PostgreSQL (замените `user`, `password`, `host`, `port`, `database_name` на ваши значения)
- `PARSER_SERVICE_URL` - URL Parser Service (по умолчанию: `http://127.0.0.1:9003`)
- `ENV` - окружение (development/production)
- `LOG_LEVEL` - уровень логирования (DEBUG/INFO/WARNING/ERROR)
- `LOG_SQL` - логировать SQL запросы (true/false)
- `CORS_ORIGINS` - разрешенные источники для CORS (разделенные запятыми)
- `ATTACHMENTS_DIR` - директория для вложений

**Важно:** Не коммитьте `.env` файлы в Git! Используйте `.env.example` для шаблона.

#### Frontend (`frontend/moderator-dashboard-ui/.env.local`)

```env
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_CHECKO_API_KEY=your_checko_api_key_here
```

**Быстрое создание (PowerShell):**
```powershell
@"
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_CHECKO_API_KEY=your_checko_api_key_here
"@ | Out-File -FilePath "frontend\moderator-dashboard-ui\.env.local" -Encoding utf8
```

#### Parser Service (`parser_service/.env`)

```env
CHROME_CDP_URL=http://127.0.0.1:9222
LOG_LEVEL=INFO
```

**Быстрое создание (PowerShell):**
```powershell
@"
CHROME_CDP_URL=http://127.0.0.1:9222
LOG_LEVEL=INFO
"@ | Out-File -FilePath "parser_service\.env" -Encoding utf8
```

### 3.4 Запуск всех сервисов

**⚠️ ВАЖНО: Рекомендуемый способ - запуск всех сервисов в одном окне терминала!**

#### Автоматический запуск (рекомендуется)

**Один клик - все сервисы запущены:**

```batch
start-all-tabby.bat
```

Это запустит:
1. Chrome CDP (если не запущен)
2. Parser Service (порт 9003)
3. Backend (порт 8000)
4. Frontend (порт 3000)
5. Монитор всех сервисов в одном окне

**Готово!** Все сервисы запущены в одном окне терминала с:
- ✅ Цветным логированием (ошибки - красным, успех - зеленым)
- ✅ Мониторингом в реальном времени
- ✅ Автоматическими health checks
- ✅ Оптимизированной нагрузкой на систему

**Откройте:** http://localhost:3000

#### Ручной запуск через PowerShell

```powershell
powershell -ExecutionPolicy Bypass -File "scripts\start-all-services-single-window.ps1" -ProjectRoot "D:\tryagain" -Mode "debug"
```

#### Режимы работы

**Debug Mode (по умолчанию):**
- Детальное логирование всех сообщений
- Быстрый мониторинг: каждые 2 секунды (startup) → 2.5 секунды (stable)
- Health checks: каждые 30 секунд (startup) → 60 секунд (stable)

**Production Mode (оптимизированный):**
- Минимальное логирование (только ошибки и предупреждения после стабилизации)
- Оптимизированный мониторинг: каждые 3.5 секунд (startup) → 5 секунд (stable)
- Health checks: каждые 70 секунд (startup) → 200 секунд (stable)
- Снижение нагрузки на систему на 60-80%

**Запуск в production mode:**
```powershell
powershell -ExecutionPolicy Bypass -File "scripts\start-all-services-single-window.ps1" -ProjectRoot "D:\tryagain" -Mode "production"
```

### 3.5 Проверка работоспособности

После запуска проверьте:

1. **Chrome CDP:**
   ```powershell
   Invoke-WebRequest http://127.0.0.1:9222/json/version
   ```

2. **Parser Service:**
   ```powershell
   Invoke-WebRequest http://127.0.0.1:9003/health
   ```

3. **Backend:**
   ```powershell
   Invoke-WebRequest http://127.0.0.1:8000/health
   ```

4. **Frontend:**
   ```powershell
   Invoke-WebRequest http://localhost:3000
   ```

Или откройте в браузере:
- Frontend: http://localhost:3000
- Backend API Docs: http://127.0.0.1:8000/docs
- Parser Service: http://127.0.0.1:9003

### 3.6 Остановка сервисов

Для остановки всех сервисов:
1. Нажмите `Ctrl+C` в окне монитора
2. Или используйте скрипт:
   ```powershell
   powershell -ExecutionPolicy Bypass -File "scripts\stop-all-services.ps1"
   ```

**Важно:** Chrome CDP не будет остановлен, если он был запущен вручную в режиме отладки.

---

## 4. Переменные окружения

### 4.1 Backend (`backend/.env`)

```env
DATABASE_URL=postgresql+asyncpg://user:password@host:port/database_name
PARSER_SERVICE_URL=http://127.0.0.1:9003
ENV=development
LOG_LEVEL=INFO
LOG_SQL=false
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
ATTACHMENTS_DIR=storage/attachments
```

**Описание:**
- `DATABASE_URL` - строка подключения к PostgreSQL (замените `user`, `password`, `host`, `port`, `database_name` на ваши значения)
- `PARSER_SERVICE_URL` - URL Parser Service (по умолчанию: `http://127.0.0.1:9003`)
- `ENV` - окружение (development/production)
- `LOG_LEVEL` - уровень логирования (DEBUG/INFO/WARNING/ERROR)
- `LOG_SQL` - логировать SQL запросы (true/false)
- `CORS_ORIGINS` - разрешенные источники для CORS (разделенные запятыми)
- `ATTACHMENTS_DIR` - директория для вложений

**Важно:** Не коммитьте `.env` файлы в Git! Используйте переменные окружения или безопасное хранение секретов для production.

### 4.2 Frontend (`frontend/moderator-dashboard-ui/.env.local`)

```env
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_CHECKO_API_KEY=your_checko_api_key_here
```

**Описание:**
- `NEXT_PUBLIC_API_URL` - URL Backend API
- `NEXT_PUBLIC_CHECKO_API_KEY` - API ключ для Checko (получить на https://checko.ru/integration/api)

### 4.3 Parser Service (`parser_service/.env`)

```env
CHROME_CDP_URL=http://127.0.0.1:9222
LOG_LEVEL=INFO
```

**Описание:**
- `CHROME_CDP_URL` - URL Chrome CDP
- `LOG_LEVEL` - уровень логирования

---

## 5. Инструменты автоматизации

### 5.1 Быстрая проверка сервисов

**Команда:**
```powershell
.\scripts\quick-check.ps1 [backend|frontend|parser|all]
```

**Примеры:**
```powershell
# Проверить все сервисы
.\scripts\quick-check.ps1 all

# Только Backend
.\scripts\quick-check.ps1 backend

# Только Frontend
.\scripts\quick-check.ps1 frontend
```

**Что проверяется:**
- **Backend**: импорты, health endpoint
- **Frontend**: линтер, доступность
- **Parser Service**: health endpoint, Chrome CDP

### 5.2 Мониторинг сервисов

**Команда:**
```powershell
.\scripts\monitor-services.ps1
```

**Что проверяется:**
- Состояние всех сервисов
- Порты и процессы
- Логи последних ошибок

### 5.3 Проверка импортов (Backend)

Перед коммитом рекомендуется запустить скрипт проверки импортов:

```powershell
python temp/backend/check_imports.py
```

Скрипт проверяет все роутеры и usecases на отсутствующие импорты `date`/`datetime`.

### 5.4 Валидация миграций

**Команда:**
```powershell
.\scripts\validate-migration.ps1 -MigrationFile "backend/migrations/XXX_migration.sql"
```

**Что проверяется:**
- Переименование последовательностей (если переименовывается таблица)
- Выдача прав доступа на последовательности (если создается SERIAL поле)

### 5.5 Проверка последовательностей после миграции

**Команда:**
```powershell
.\scripts\check-sequences-after-migration.ps1 -TableName <table_name>
```

**Что проверяется:**
- Последовательность имеет правильное имя
- Последовательность имеет права доступа
- Тестирование сохранения данных в таблицу

---

## 6. Структура проекта (краткая версия)

### 6.0 Временные файлы и эксперименты

**⚠️ ВАЖНО: Правила организации временных файлов**

- **Все временные скрипты, debug-код, проверки гипотез, старые версии интерфейса:**
  - Складывать только в `temp/` или `temp/experiments/`
  
- **Запрещено создавать тестовые файлы и директории в:**
  - `backend/app/**`
  - `frontend/moderator-dashboard-ui/**`
  - `parser_service/src/**`
  
- **Если эксперимент стал постоянной частью системы:**
  - Перенести его из `temp/` в нужный рабочий каталог
  - Обновить `docs/PROJECT_MAP.md` и, при необходимости, `docs/MASTER_INSTRUCTION.md`

**Структура `temp/`:**
- `temp/backend/` - временные скрипты для Backend
- `temp/experiments/` - экспериментальные версии и дизайн-проекты
  - `moderator-dashboardNEW/` - старая версия frontend
  - `supplier-card-design/` - дизайн-проект карточки поставщика
  - `supplier-moderation-dashboard/` - старая версия dashboard

## 6. Структура проекта (краткая версия)

### 6.1 Backend

**Основные директории:**
- `app/transport/routers/` - FastAPI endpoints (только маршрутизация)
- `app/usecases/` - бизнес-логика (чистая логика без HTTP)
- `app/adapters/` - внешние сервисы (Parser Service, Checko, БД)
- `app/db/` - работа с БД (модели, репозитории, сессии)
- `migrations/` - SQL миграции БД

**Ключевые файлы:**
- `app/main.py` - точка входа FastAPI приложения
- `app/config.py` - конфигурация (настройки, переменные окружения)

### 6.2 Frontend

**Основные директории:**
- `app/` - Next.js App Router страницы
- `components/` - React компоненты
- `lib/` - утилиты и API клиент

**Ключевые файлы:**
- `lib/api.ts` - API клиент (все HTTP запросы)
- `lib/types.ts` - TypeScript типы
- `lib/utils-domain.ts` - утилиты для работы с доменами
- `lib/cache.ts` - кэширование данных

### 6.3 Parser Service

**Основные директории:**
- `src/` - основной код
  - `parser.py` - основной класс Parser
  - `engines.py` - классы поисковых движков (GoogleEngine, YandexEngine)
  - `cdp_client.py` - клиент для Chrome CDP

**Ключевые файлы:**
- `api.py` - FastAPI приложение Parser Service
- `run_api.py` - скрипт запуска API

**Полная карта проекта:** см. [`PROJECT_MAP.md`](PROJECT_MAP.md)

---

## 7. База данных

### 7.1 Основные таблицы

- `moderator_suppliers` - поставщики
- `keywords` - ключевые слова
- `supplier_keywords` - связь поставщиков и ключевых слов
- `moderator_blacklist` - черный список доменов
- `parsing_runs` - запуски парсинга
- `domains_queue` - очередь доменов
- `audit_log` - журнал аудита изменений

### 7.2 Миграции

**Расположение:** `backend/migrations/`

**Применение:**
```powershell
# Установите переменную окружения PGPASSWORD
$env:PGPASSWORD="your_postgres_password"

# Примените миграцию
psql -U postgres -d your_database_name -h localhost -p 5432 -f backend\migrations\001_initial_schema.sql
```

**Валидация:** `scripts/validate-migration.ps1`

**Важно:** 
- Все изменения схемы БД должны быть через миграции
- Используйте переменные окружения для паролей, не жёстко вписывайте их в команды

### 7.3 Резервное копирование

**Создание бэкапа:**
```powershell
# Установите переменную окружения PGPASSWORD
$env:PGPASSWORD="your_postgres_password"

# Создайте бэкап
pg_dump -U postgres -d your_database_name -h localhost -p 5432 > backup_$(Get-Date -Format "yyyyMMdd_HHmmss").sql
```

**Восстановление из бэкапа:**
```powershell
# Установите переменную окружения PGPASSWORD
$env:PGPASSWORD="your_postgres_password"

# Восстановите из бэкапа
psql -U postgres -d your_database_name -h localhost -p 5432 < backup_20251226_120000.sql
```

**Примечание:** Замените `your_postgres_password` и `your_database_name` на ваши реальные значения из переменных окружения.

---

## 8. Разработка

### 8.1 Backend тесты

```powershell
cd backend
pytest
```

### 8.2 Frontend линтинг

```powershell
cd frontend/moderator-dashboard-ui
npm run lint
npm run type-check
```

### 8.3 Проверка импортов (Backend)

Перед коммитом рекомендуется запустить скрипт проверки импортов:

```powershell
python temp/backend/check_imports.py
```

### 8.4 Чеклист перед коммитом

- [ ] Запустить `.\scripts\quick-check.ps1 all`
- [ ] Проверить, что нет ошибок линтера
- [ ] **ОБЯЗАТЕЛЬНО проверить через реальный фронтенд** (если применимо)
  - Запустить Frontend: `cd frontend/moderator-dashboard-ui && npm run dev`
  - Открыть изменённую страницу в браузере
  - Выполнить действия, которые используют изменённую функциональность
  - **Убедиться, что результат запускается и отображается так, как это задумано (И ОЖИДАЕТСЯ)**
  - Проверить консоль браузера (F12) - не должно быть ошибок
- [ ] Задокументировать ошибки (если были)

**⚠️ КРИТИЧЕСКИ ВАЖНО: Проверка на фронтенде - это MUST HAVE! Это правило не может быть проигнорировано!**

---

## 9. Ссылки на другие документы

### 9.1 Для решения проблем

- **`TROUBLESHOOTING.md`** - библия ошибок и решений
  - ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ перед диагностикой
  - Чеклист диагностики
  - Все ошибки и их решения

### 9.2 Для критических изменений

- **`CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md`** - критические точки интеграции и чеклисты
  - Критические точки интеграции (blacklist, группировка доменов, определение источников)
  - Чеклисты проверки изменений
  - Чеклисты для миграций
  - FAQ по последовательностям PostgreSQL

### 9.3 Для детальной информации

- **`PROJECT_SPECIFICATION.md`** - детальная спецификация API и рабочих связок Backend-Frontend
- **`PROJECT_MAP.md`** - полная карта структуры проекта
- **`SOURCE_DETERMINATION_FLOW.md`** - поток данных определения источников
- **`TASK_CHECKLISTS.md`** - чеклисты для разных типов задач
- **`ARCHITECTURE_RECOMMENDATIONS.md`** - рекомендации по архитектуре

---

## 10. Архивная документация

Следующие файлы помечены как архивные. Актуальная информация находится в этом документе (`MASTER_INSTRUCTION.md`):

### Файлы запуска и настройки (архив)
**Расположение:** `docs/archive/`

- `docs/archive/QUICK_START.md` - быстрый старт (информация включена в раздел 3)
- `docs/archive/START_HERE.md` - точка входа (информация включена в раздел 3)
- `docs/archive/README_START.md` - запуск в 2 клика (информация включена в раздел 3)
- `docs/archive/DATABASE_SETUP_GUIDE.md` - настройка БД (информация включена в раздел 3.2)
- `docs/archive/CREATE_ENV.md` - создание .env файлов (информация включена в раздел 3.3)
- `docs/archive/APPLY_MIGRATIONS.md` - применение миграций (информация включена в раздел 3.2)

### Статусные файлы (архив)
**Расположение:** `docs/archive/`

- `docs/archive/FIXED_ISSUES.md` - исправленные проблемы (актуальная информация в `docs/TROUBLESHOOTING.md`)
- `docs/archive/LAUNCH_STATUS.md` - статус запуска серверов (актуальная информация в этом документе, раздел 3.4)
- `docs/archive/RUN_STATUS.md` - статус запуска серверов (актуальная информация в этом документе, раздел 3.4)

### Документация (архив)
- `docs/DOCUMENTATION_INDEX.md` - индекс документации (заменен на этот файл)
- `docs/README.md` - описание документации (заменен на этот файл)

### Рекомендации (архив)
- `docs/RECOMMENDATIONS.md` - рекомендации для повышения эффективности (исторический документ)
- `docs/CHECKLIST_IMPLEMENTATION.md` - чеклист для проверки реализации (устарел, заменен на TASK_CHECKLISTS.md)
- `docs/IMPROVEMENTS_2025_01_27.md` - выполненные рекомендации от 27 января 2025 (исторический документ)

### Версии промптов (архив)
- `docs/V0_*.md` - исторические версии промптов

### Экспериментальные версии (архив)
- `temp/experiments/moderator-dashboardNEW/` - старая версия frontend
- `temp/experiments/supplier-card-design/` - дизайн-проект карточки поставщика
- `temp/experiments/supplier-moderation-dashboard/` - старая версия dashboard

**⚠️ Примечание:** Архивные файлы сохранены для истории, но актуальная информация находится в этом документе. Экспериментальные версии перенесены в `temp/experiments/` для сохранения истории разработки.

---

## 11. Обновление документации

**⚠️ ВАЖНО:** Любое изменение кода, которое меняет поведение системы, должно сопровождаться обновлением соответствующих разделов документации (см. правила в `.cursorrules`).

Это включает:
- Изменения API endpoints → обновить `docs/PROJECT_SPECIFICATION.md`
- Изменения структуры БД → обновить `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md` и раздел БД в этом документе
- Изменения критических связок → обновить `docs/PROJECT_SPECIFICATION.md` и `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md`
- Новые ошибки → задокументировать в `docs/TROUBLESHOOTING.md`

---

**Дата создания:** 2025-12-29  
**Последнее обновление:** 2025-12-29

