# Рекомендации для повышения эффективности работы

**Дата создания:** 2025-12-27  
**Последнее обновление:** 2025-12-27

---

## 🎯 Приоритет 1: Критически важные улучшения

### 1. Автоматическая проверка миграций перед применением

**Проблема:** Миграции могут ломать существующие данные или структуру БД.

**Решение:**
- Создать скрипт `scripts/check_migration.sql` для проверки миграций
- Добавить проверку существования таблиц/колонок перед изменением
- Создать backup перед применением миграций

**Пример:**
```sql
-- scripts/check_migration.sql
-- Проверка перед применением миграции
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'domains_queue') THEN
        RAISE EXCEPTION 'Table domains_queue does not exist';
    END IF;
END $$;
```

**Команда:**
```bash
# Проверка миграции перед применением
psql -U postgres -d b2bplatform -f scripts/check_migration.sql
psql -U postgres -d b2bplatform -f backend/migrations/004_fix_domains_queue_primary_key.sql
```

---

### 2. Автоматическая выдача прав доступа в миграциях

**Проблема:** После переименования таблиц нужно вручную выдавать права доступа.

**Решение:**
- Добавить в каждую миграцию автоматическую выдачу прав
- Использовать `GRANT ALL PRIVILEGES` для всех таблиц
- Добавить в шаблон миграции

**Пример (уже добавлено в 004_fix_domains_queue_primary_key.sql):**
```sql
-- В конце каждой миграции
GRANT ALL PRIVILEGES ON TABLE domains_queue TO postgres;
GRANT ALL PRIVILEGES ON TABLE domains_queue TO PUBLIC;
ALTER TABLE domains_queue OWNER TO postgres;
```

---

### 3. Улучшение фильтрации в API endpoints

**Проблема:** Фильтрация по `parsingRunId` может не работать из-за проблем с alias в FastAPI.

**Решение:**
- Использовать явное имя параметра вместо alias
- Добавить валидацию параметров фильтрации
- Добавить логирование для отладки

**Пример:**
```python
# backend/app/transport/routers/domains_queue.py
@router.get("/queue")
async def list_domains_queue_endpoint(
    parsingRunId: Optional[str] = Query(default=None),  # Явное имя параметра
    db: AsyncSession = Depends(get_db)
):
    # Логирование для отладки
    logger.info(f"Filtering by parsingRunId={parsingRunId}")
    # ...
```

---

## 🚀 Приоритет 2: Улучшение процесса разработки

### 4. Автоматическая проверка структуры БД после миграций

**Проблема:** После миграций нужно вручную проверять структуру таблиц.

**Решение:**
- Создать скрипт `scripts/verify_migration.py` для проверки структуры
- Проверять наличие всех колонок, индексов, constraints
- Автоматически проверять после применения миграции

**Пример:**
```python
# scripts/verify_migration.py
import asyncio
from sqlalchemy import inspect
from app.adapters.db.session import AsyncSessionLocal
from app.adapters.db.models import DomainQueueModel

async def verify_migration():
    async with AsyncSessionLocal() as session:
        inspector = inspect(DomainQueueModel.__table__)
        # Проверка primary key
        assert 'id' in [col.name for col in inspector.primary_key.columns]
        # Проверка unique constraint
        # ...
```

---

### 5. Улучшение логирования для отладки фильтрации

**Проблема:** Сложно понять, почему фильтрация не работает без логов.

**Решение:**
- Добавить структурированное логирование во все репозитории
- Логировать все параметры фильтрации
- Логировать SQL запросы (в development режиме)

**Пример:**
```python
# backend/app/adapters/db/repositories.py
import logging
logger = logging.getLogger(__name__)

async def list(self, parsing_run_id: Optional[str] = None):
    logger.info(f"DomainQueueRepository.list called with parsing_run_id={parsing_run_id}")
    query = select(DomainQueueModel)
    if parsing_run_id:
        logger.debug(f"Applying filter: parsing_run_id={parsing_run_id}")
        query = query.where(DomainQueueModel.parsing_run_id == parsing_run_id)
    # ...
```

---

### 6. Создание тестов для проверки фильтрации

**Проблема:** Фильтрация не тестируется автоматически.

**Решение:**
- Создать unit тесты для репозиториев
- Создать integration тесты для API endpoints
- Добавить тесты для проверки фильтрации по `parsingRunId` и `keyword`

**Пример:**
```python
# backend/tests/integration/test_domains_queue_filtering.py
async def test_filter_by_parsing_run_id():
    # Создать тестовые данные
    # Проверить фильтрацию
    # Убедиться, что возвращаются только записи для конкретного runId
    pass
```

---

## 🔧 Приоритет 3: Оптимизация и улучшение кода

### 7. Улучшение обработки NULL значений в фильтрации

**Проблема:** Фильтрация может не работать правильно с NULL значениями.

**Решение:**
- Добавить явную обработку NULL в запросах
- Использовать `IS NULL` / `IS NOT NULL` вместо `== None`
- Добавить валидацию параметров

**Пример:**
```python
# backend/app/adapters/db/repositories.py
if parsing_run_id:
    if parsing_run_id.lower() == 'null':
        query = query.where(DomainQueueModel.parsing_run_id.is_(None))
    else:
        query = query.where(DomainQueueModel.parsing_run_id == parsing_run_id)
```

---

### 8. Улучшение сериализации DTO для Frontend

**Проблема:** DTO могут возвращать snake_case вместо camelCase.

**Решение:**
- Использовать `model_dump(by_alias=True)` для всех DTO
- Добавить в базовый класс `BaseDTO`
- Проверить все endpoints на правильную сериализацию

**Пример:**
```python
# backend/app/transport/schemas/common.py
class BaseDTO(BaseModel):
    def model_dump(self, **kwargs):
        kwargs.setdefault("by_alias", True)
        kwargs.setdefault("mode", "json")
        return super().model_dump(**kwargs)
```

---

### 9. Создание скрипта для проверки целостности данных

**Проблема:** После миграций нужно проверять целостность данных.

**Решение:**
- Создать скрипт `scripts/check_data_integrity.py`
- Проверять связи между таблицами
- Проверять уникальные constraints
- Проверять наличие orphaned записей

**Пример:**
```python
# scripts/check_data_integrity.py
async def check_domains_queue_integrity():
    # Проверить, что все записи имеют parsing_run_id
    # Проверить, что нет дубликатов по (domain, keyword, parsing_run_id)
    # Проверить связи с parsing_runs
    pass
```

---

## 📊 Приоритет 4: Мониторинг и диагностика

### 10. Добавление метрик для парсинга

**Проблема:** Нет метрик для отслеживания производительности парсинга.

**Решение:**
- Добавить счетчики успешных/неуспешных парсингов
- Добавить время выполнения парсинга
- Добавить количество найденных доменов

**Пример:**
```python
# backend/app/usecases/start_parsing.py
import time
start_time = time.time()
# ... парсинг ...
duration = time.time() - start_time
logger.info(f"Parsing completed in {duration:.2f}s, found {saved_count} domains")
```

---

### 11. Улучшение обработки ошибок при парсинге

**Проблема:** Ошибки парсинга не всегда понятны.

**Решение:**
- Добавить детальное логирование ошибок
- Добавить retry механизм для временных ошибок
- Добавить уведомления пользователю о статусе парсинга

**Пример:**
```python
# backend/app/usecases/start_parsing.py
try:
    result = await parser_client.parse(...)
except httpx.RequestError as e:
    logger.error(f"Parser Service connection error: {e}", exc_info=True)
    # Retry logic
    for attempt in range(3):
        try:
            result = await parser_client.parse(...)
            break
        except:
            if attempt == 2:
                raise
```

---

## 🎨 Приоритет 5: Улучшение UX

### 12. Улучшение отображения результатов парсинга

**Проблема:** Результаты парсинга могут быть неочевидными для пользователя.

**Решение:**
- Добавить индикатор прогресса парсинга
- Добавить фильтры для результатов
- Добавить экспорт результатов в CSV/Excel

**Пример:**
```typescript
// frontend/moderator-dashboard-ui/app/parsing-runs/[runId]/page.tsx
// Добавить индикатор прогресса
{run.status === "running" && (
  <Progress value={progress} />
)}
```

---

### 13. Добавление валидации на Frontend

**Проблема:** Нет валидации параметров на Frontend перед отправкой.

**Решение:**
- Добавить валидацию ключевых слов (минимальная длина, запрещенные символы)
- Добавить валидацию глубины парсинга (диапазон значений)
- Добавить предупреждения перед запуском парсинга

**Пример:**
```typescript
// frontend/moderator-dashboard-ui/app/manual-parsing/page.tsx
if (keyword.length < 2) {
  setError("Ключевое слово должно содержать минимум 2 символа");
  return;
}
if (depth < 1 || depth > 10) {
  setError("Глубина парсинга должна быть от 1 до 10");
  return;
}
```

---

## 📝 Чеклист для применения рекомендаций

### Немедленно (Приоритет 1):
- [ ] Добавить автоматическую выдачу прав в шаблон миграции
- [ ] Исправить фильтрацию по `parsingRunId` в API
- [ ] Добавить проверку миграций перед применением

### В ближайшее время (Приоритет 2):
- [ ] Создать скрипт проверки структуры БД
- [ ] Улучшить логирование для отладки
- [ ] Создать тесты для фильтрации

### В будущем (Приоритет 3-5):
- [ ] Улучшить обработку NULL значений
- [ ] Улучшить сериализацию DTO
- [ ] Добавить метрики для парсинга
- [ ] Улучшить UX для результатов парсинга

---

## 🔗 Связанные документы

- **Библия ошибок:** [`docs/TROUBLESHOOTING.md`](TROUBLESHOOTING.md)
- **Спецификация проекта:** [`docs/PROJECT_SPECIFICATION.md`](PROJECT_SPECIFICATION.md)
- **Индекс документации:** [`docs/DOCUMENTATION_INDEX.md`](DOCUMENTATION_INDEX.md)

---

**Примечание:** Эти рекомендации основаны на опыте работы с проектом и исправлении ошибок. Приоритеты могут изменяться в зависимости от текущих потребностей проекта.





