# 📚 DOMAIN PARSER - Полная документация интеграции

## 🎯 Описание системы

**Domain Parser** - это система автоматического извлечения ИНН и email адресов с веб-сайтов компаний. Система интегрирована в проект для автоматизации сбора контактных данных поставщиков.

---

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Next.js)                       │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  parsing-runs/[runId]/page.tsx                             │ │
│  │  - Кнопка "Получить данные"                                │ │
│  │  - Индикаторы статуса (🔄 ✅ ❌)                           │ │
│  │  - Прогресс-бар обработки                                  │ │
│  │  - Индикаторы I (ИНН) и @ (Email)                         │ │
│  │  - Предзаполнение формы поставщика                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↕ HTTP                              │
└─────────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                        BACKEND (FastAPI)                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  /domain-parser/extract-batch (POST)                       │ │
│  │  - Принимает список доменов                                │ │
│  │  - Запускает background task                               │ │
│  │  - Возвращает parserRunId                                  │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  /domain-parser/status/{parserRunId} (GET)                 │ │
│  │  - Возвращает статус парсинга                              │ │
│  │  - Прогресс (processed/total)                              │ │
│  │  - Результаты для каждого домена                           │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↕                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Background Task (_process_domain_parser_batch)            │ │
│  │  - Обрабатывает домены последовательно                     │ │
│  │  - Обновляет статус в памяти                               │ │
│  │  - Сохраняет результаты в БД                               │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↕ subprocess                        │
└─────────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                   DOMAIN PARSER (Python + Playwright)            │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  domain_info_parser/parser.py                              │ │
│  │  - DomainInfoParser класс                                  │ │
│  │  - Запускает Playwright браузер                            │ │
│  │  - Парсит главную страницу                                 │ │
│  │  - Ищет контактные страницы                                │ │
│  │  - Извлекает ИНН (regex)                                   │ │
│  │  - Извлекает Email (regex)                                 │ │
│  │  - Возвращает JSON результат                               │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📂 Структура файлов

### **Backend (FastAPI)**

```
backend/
├── app/
│   ├── transport/
│   │   ├── routers/
│   │   │   └── domain_parser.py          # API endpoints
│   │   └── schemas/
│   │       └── domain_parser.py          # Pydantic schemas
│   └── main.py                           # Регистрация роутера
```

### **Frontend (Next.js)**

```
frontend/moderator-dashboard-ui/
├── app/
│   └── parsing-runs/
│       └── [runId]/
│           └── page.tsx                  # UI интеграция
├── lib/
│   ├── api.ts                            # API функции
│   └── types.ts                          # TypeScript типы
```

### **Parser (Python)**

```
domain_info_parser/
├── parser.py                             # Основной парсер
├── run_parser.py                         # Скрипт запуска
├── test_parser.py                        # Тесты
├── requirements.txt                      # Зависимости
└── README.md                             # Документация парсера
```

---

## 🔌 API Endpoints

### **1. POST /domain-parser/extract-batch**

Запускает парсинг доменов для извлечения ИНН и email.

**Request:**
```json
{
  "runId": "a0097613-61ab-4831-8d48-ef9c8cbfac8b",
  "domains": ["example.com", "test.ru"]
}
```

**Response:**
```json
{
  "runId": "a0097613-61ab-4831-8d48-ef9c8cbfac8b",
  "parserRunId": "parser_20260110_165421_d526162b"
}
```

**Логика:**
1. Проверяет существование parsing run
2. Генерирует уникальный `parserRunId`
3. Инициализирует статус в памяти
4. Запускает background task
5. Возвращает `parserRunId` для polling

---

### **2. GET /domain-parser/status/{parserRunId}**

Получает статус парсинга и результаты.

**Response:**
```json
{
  "runId": "a0097613-61ab-4831-8d48-ef9c8cbfac8b",
  "parserRunId": "parser_20260110_165421_d526162b",
  "status": "running",
  "processed": 2,
  "total": 5,
  "results": [
    {
      "domain": "example.com",
      "inn": "7810953727",
      "emails": ["info@example.com"],
      "sourceUrls": ["https://example.com/contacts"],
      "error": null
    }
  ]
}
```

**Статусы:**
- `running` - парсинг в процессе
- `completed` - парсинг завершен
- `failed` - ошибка парсинга

---

## 💾 Структура данных

### **DomainParserResult**

```typescript
interface DomainParserResult {
  domain: string              // Доменное имя
  inn: string | null          // ИНН (10 или 12 цифр)
  emails: string[]            // Массив email адресов
  sourceUrls: string[]        // URL где найдены данные
  error?: string | null       // Ошибка (если есть)
}
```

### **Сохранение в БД**

Результаты сохраняются в `process_log` таблицы `parsing_runs`:

```json
{
  "domain_parser": {
    "runs": {
      "parser_20260110_165421_d526162b": {
        "status": "completed",
        "started_at": "2026-01-10T16:54:21",
        "finished_at": "2026-01-10T16:55:30",
        "results": [...]
      }
    }
  }
}
```

---

## 🎨 UI Компоненты

### **1. Кнопка "Получить данные"**

```tsx
<Button
  size="sm"
  onClick={handleDomainParser}
  disabled={parserLoading}
  className="h-8 text-xs bg-blue-600 hover:bg-blue-700"
>
  <FileSearch className="h-3 w-3 mr-1" />
  Получить данные ({selectedDomains.size})
</Button>
```

**Логика:**
- Фильтрует домены (только без поставщиков)
- Запускает парсер через API
- Показывает toast уведомления

---

### **2. Индикатор статуса**

```tsx
{parserRunId && parserStatus && (
  <div className="text-xs mb-2">
    <div className="flex items-center gap-2">
      <span className={`font-medium ${
        parserStatus.status === 'running' ? 'text-blue-600' : 
        parserStatus.status === 'completed' ? 'text-green-600' : 
        'text-red-600'
      }`}>
        {parserStatus.status === 'running' ? '🔄 Получение данных...' : 
         parserStatus.status === 'completed' ? '✅ Данные получены' : 
         '❌ Ошибка'}
      </span>
      <span className="text-muted-foreground">
        {parserStatus.processed}/{parserStatus.total} доменов
      </span>
    </div>
    {parserStatus.status === 'running' && (
      <div className="mt-1 w-full bg-gray-200 rounded-full h-2">
        <div 
          className="bg-blue-600 h-2 rounded-full transition-all duration-300"
          style={{ width: `${(parserStatus.processed / parserStatus.total) * 100}%` }}
        />
      </div>
    )}
  </div>
)}
```

**Отображение:**
- 🔄 **Получение данных...** 2/5 доменов
- [████████████░░░░░░░░] 40%

---

### **3. Индикаторы I и @**

```tsx
{(() => {
  const parserResult = parserResultsMap.get(group.domain)
  if (!parserResult || parserResult.error) return null
  return (
    <>
      {parserResult.inn && (
        <span 
          className="ml-1 px-1.5 py-0.5 bg-blue-100 text-blue-700 rounded text-xs font-bold"
          title={`ИНН получен: ${parserResult.inn}`}
        >
          I
        </span>
      )}
      {parserResult.emails && parserResult.emails.length > 0 && (
        <span 
          className="ml-1 px-1.5 py-0.5 bg-green-100 text-green-700 rounded text-xs font-bold"
          title={`Email получен: ${parserResult.emails.join(', ')}`}
        >
          @
        </span>
      )}
    </>
  )
})()}
```

**Отображение:**
- **example.com** [I] [@] - ИНН и Email получены
- **test.ru** [@] - Только Email получен
- **demo.com** [I] - Только ИНН получен

---

### **4. Предзаполнение формы поставщика**

```tsx
// При открытии диалога создания поставщика
const parserResult = parserResultsMap.get(rootDomain) || parserResultsMap.get(domain)

let prefillInn = ""
let prefillEmail = ""

if (parserResult && !parserResult.error) {
  prefillInn = parserResult.inn || ""
  prefillEmail = parserResult.emails && parserResult.emails.length > 0 ? parserResult.emails[0] : ""
}

setSupplierForm({
  name: "",
  inn: prefillInn,        // ✅ Автоматически заполнено
  email: prefillEmail,    // ✅ Автоматически заполнено
  domain: domain,
  type: type,
  // ...
})
```

---

## 🔄 Workflow (Пошаговый процесс)

### **Шаг 1: Пользователь выделяет домены**

```
☐ example.com    1 URL
☑ test.ru        2 URLs
☑ demo.com       1 URL
```

### **Шаг 2: Нажимает "Получить данные"**

```javascript
// Frontend фильтрует домены
const domainsWithoutSuppliers = domainsArray.filter(domain => {
  const rootDomain = extractRootDomain(domain).toLowerCase()
  const supplier = suppliersByDomainRef.current.get(rootDomain)
  return !supplier // Только домены БЕЗ поставщиков
})

// Запускает API
const resp = await startDomainParserBatch(runId, domainsWithoutSuppliers)
setParserRunId(resp.parserRunId)
```

### **Шаг 3: Backend запускает парсер**

```python
# Backend создает background task
background_tasks.add_task(_process_domain_parser_batch, parser_run_id, run_id, domains)

# Task обрабатывает домены последовательно
for i, domain in enumerate(domains):
    result = await _run_domain_parser_for_domain(domain)
    results.append(result)
    
    # Обновляет статус
    _parser_runs[parser_run_id]["processed"] = i + 1
    _parser_runs[parser_run_id]["results"] = results
```

### **Шаг 4: Парсер извлекает данные**

```python
# Запускает Playwright браузер
parser = DomainInfoParser(headless=True, timeout=15000)
await parser.start()

# Парсит домен
result = await parser.parse_domain(domain)
# {
#   "domain": "example.com",
#   "inn": "7810953727",
#   "emails": ["info@example.com"],
#   "source_urls": ["https://example.com/contacts"]
# }
```

### **Шаг 5: Frontend отображает прогресс**

```
🔄 Получение данных... 2/3 доменов
[████████████████████░░░░░░░░░░] 67%

☑ test.ru      [I] [@]
☑ demo.com     [@]
☐ example.com  (обрабатывается...)
```

### **Шаг 6: Парсинг завершен**

```
✅ Данные получены 3/3 доменов

☑ test.ru      [I] [@]
☑ demo.com     [@]
☑ example.com  [I] [@]
```

### **Шаг 7: Создание поставщика**

```
Пользователь нажимает "Поставщик" или "Реселлер"

Форма автоматически заполняется:
┌─────────────────────────────────┐
│ Название: [                   ] │
│ ИНН:      [7810953727         ] │ ✅ Предзаполнено
│ Email:    [info@example.com   ] │ ✅ Предзаполнено
│ Домен:    [example.com        ] │
└─────────────────────────────────┘
```

---

## 🧪 Тестирование

### **Тест 1: API Endpoint**

```bash
cd d:\tryagain
python test_domain_parser_integration.py
```

**Ожидаемый результат:**
```
✅ Parser запущен
✅ Парсинг завершен
📊 Найдено данных: 2/3 доменов
```

### **Тест 2: UI Индикация**

```bash
python test_parser_ui.py
```

**Ожидаемый результат:**
```
[██████████████████████████████] 100% (3/3) - completed
✅ Данные получены
```

### **Тест 3: Парсер (standalone)**

```bash
cd domain_info_parser
python test_parser.py
```

**Ожидаемый результат:**
```
✅ ИНН найден: 3 (60%)
✅ Email найден: 4 (80%)
🎉 ТЕСТ ПРОЙДЕН!
```

---

## 🐛 Troubleshooting

### **Проблема: Парсер не запускается**

**Симптомы:**
- Кнопка "Получить данные" не реагирует
- Нет индикатора статуса

**Решение:**
1. Проверьте, что backend запущен: `netstat -ano | findstr :8000`
2. Проверьте логи backend: `backend/logs/`
3. Проверьте browser console: F12 → Console

---

### **Проблема: Парсер завершается с ошибкой**

**Симптомы:**
- Статус "❌ Ошибка"
- В результатах `error` не null

**Решение:**
1. Проверьте, что Playwright установлен: `playwright install chromium`
2. Проверьте логи backend
3. Проверьте доступность домена

---

### **Проблема: Индикаторы I и @ не отображаются**

**Симптомы:**
- Парсинг завершен, но индикаторов нет

**Решение:**
1. Обновите страницу (F5)
2. Проверьте `parserResultsMap` в React DevTools
3. Проверьте, что polling работает (каждые 2 сек)

---

## 📊 Производительность

### **Время обработки**

| Количество доменов | Среднее время | Время на домен |
|-------------------|---------------|----------------|
| 1 домен           | ~10 сек       | 10 сек         |
| 5 доменов         | ~50 сек       | 10 сек         |
| 10 доменов        | ~100 сек      | 10 сек         |
| 50 доменов        | ~8 минут      | 10 сек         |

### **Успешность извлечения**

| Данные | Процент успеха |
|--------|----------------|
| Email  | 60-80%         |
| ИНН    | 30-50%         |
| Оба    | 30-40%         |

---

## 🔐 Безопасность

### **Backend**

- ✅ Валидация входных данных (Pydantic)
- ✅ Проверка существования parsing run
- ✅ Таймаут парсинга (30 сек на домен)
- ✅ Обработка ошибок subprocess

### **Frontend**

- ✅ Фильтрация доменов (только без поставщиков)
- ✅ Disabled кнопки во время парсинга
- ✅ Обработка ошибок API
- ✅ Валидация данных перед отправкой

### **Parser**

- ✅ Headless режим браузера
- ✅ Таймаут загрузки страниц (15 сек)
- ✅ Фильтрация email (исключает test@, example@)
- ✅ Валидация ИНН (10 или 12 цифр)

---

## 🚀 Deployment

### **Production Checklist**

- [ ] Backend запущен и доступен
- [ ] Frontend собран (`npm run build`)
- [ ] Playwright установлен (`playwright install chromium`)
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Логирование настроено
- [ ] Мониторинг настроен

---

## 📝 Changelog

### **v1.0.0 (2026-01-10)**

- ✅ Создан Domain Parser с Playwright
- ✅ Интегрирован backend API (FastAPI)
- ✅ Интегрирован frontend UI (Next.js)
- ✅ Добавлена визуальная индикация прогресса
- ✅ Добавлены индикаторы I (ИНН) и @ (Email)
- ✅ Добавлено предзаполнение формы поставщика
- ✅ Создана полная документация

---

## 👥 Контакты

**Разработчик:** Cascade AI Assistant  
**Дата:** 10 января 2026  
**Версия:** 1.0.0

---

## 📚 Дополнительные ресурсы

- [Playwright Documentation](https://playwright.dev/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Next.js Documentation](https://nextjs.org/docs)
- [domain_info_parser/README.md](domain_info_parser/README.md)

---

**🎉 Domain Parser готов к использованию!**
