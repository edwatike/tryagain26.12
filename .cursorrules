# Rule Type: Always
# B2B Platform - Project Rules for Cursor

  ## 🚨 ВАЖНО: ВХОДНАЯ ТОЧКА ДЛЯ НОВОГО ЧАТА

**Перед началом работы в новом чате ОБЯЗАТЕЛЬНО:**

1. **Прочитай `docs/DOCUMENTATION_INDEX.md`** - главная точка входа в документацию
2. **Прочитай `docs/TROUBLESHOOTING.md`** - Библия ошибок и решений (НЕПРИКОСНОВЕННАЯ!)
   - **ОБЯЗАТЕЛЬНО изучи раздел "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ"** - это первое, что нужно делать при диагностике
   - **ОБЯЗАТЕЛЬНО изучи раздел "Рекомендации для быстрой диагностики проблем"** - используй эти команды при диагностике
3. **Прочитай `README.md`** - общее описание проекта
4. **Изучи структуру проекта** через `docs/DOCUMENTATION_INDEX.md`

**Документация проекта:**
- **Индекс документации**: `docs/DOCUMENTATION_INDEX.md` - НАЧНИ ОТСЮДА!
- **Библия ошибок**: `docs/TROUBLESHOOTING.md` - все ошибки и решения
- **Правила работы**: `.cursorrules` (этот файл)
- **Общее описание**: `README.md`

**При возникновении ошибки:**
- **СНАЧАЛА выполни проверки из `docs/TROUBLESHOOTING.md` раздел "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ"** - проверь состояние всех сервисов
- **СНАЧАЛА используй чеклист из `docs/TROUBLESHOOTING.md` раздел "7. Чеклист диагностики"** - проверь все 8 пунктов
- Затем проверь `docs/TROUBLESHOOTING.md` на наличие похожих проблем
- Если решения нет - реши и ОБЯЗАТЕЛЬНО задокументируй в `docs/TROUBLESHOOTING.md`

## Overview

- Purpose: B2B-платформа с личным кабинетом модератора, парсингом сайтов поставщиков и интеграцией с Checko.
- Stack:
  - Backend: FastAPI, PostgreSQL, SQLAlchemy, Pydantic v2
  - Frontend: Next.js (App Router), TypeScript, Tailwind, Shadcn UI
  - Parser Service: Python, Chrome CDP (Playwright)
- Key docs: `docs/DOCUMENTATION_INDEX.md`, `docs/TROUBLESHOOTING.md`, `README.md`

Перед любыми изменениями:
1) Найди и прочитай `docs/DOCUMENTATION_INDEX.md` (главная точка входа).
2) Прочитай `docs/TROUBLESHOOTING.md` (проверь известные проблемы).
3) Прочитай `README.md` (общее описание проекта).
4) Если задача затрагивает конкретную часть (frontend/backend/parser), прочитай соответствующий раздел документации.

## ⚠️ ВАЖНО: Организация файлов

**ЗАПРЕЩЕНО:**
- Создавать временные файлы и скрипты в корне проекта
- Создавать тестовые файлы для проверки гипотез в основных директориях
- Засорять проект временными файлами (test.py, check.py, temp.py и т.д.)

**ОБЯЗАТЕЛЬНО:**
- Все временные скрипты, проверки гипотез, тестовые файлы создавать в отдельной папке `temp/` или `experiments/`
- Временные файлы должны быть четко отделены от основного кода проекта
- После завершения работы с временными файлами - удалять их или перемещать в архив

**НЕ УСТРАИВАТЬ ПОМОЙКУ В ПРОЕКТЕ!**

## Architecture Principles

- Три отдельных сервиса:
  - `backend/` — API и бизнес-логика.
  - `frontend/moderator-dashboard-ui/` — личный кабинет модератора.
  - `parser_service/` — парсинг через Chrome CDP.
- Не перемешивать ответственность между сервисами.
- Сначала описывать/уточнять архитектурные изменения текстом, потом менять код.

## Backend Rules (FastAPI)

- Основные директории:
  - `app/transport/routers` — только FastAPI endpoints.
  - `app/usecases` — бизнес-логика.
  - `app/adapters` — внешние сервисы (Parser Service, Checko, Ollama).
  - `app/db` — модели и работа с БД.
- Запрещено:
  - Писать SQL и сложную логику прямо в роутерах.
  - Менять структуру таблиц без миграций.
  - Переименовывать поля DTO без обновления Pydantic-схем и TS-типов фронта.
- Перед изменением эндпоинтов:
  - Открой `/docs` и посмотри, какие маршруты уже существуют.
  - Не ломай существующие URL без явного запроса.

**⚠️ КРИТИЧЕСКИ ВАЖНО: Обязательные проверки миграций с последовательностями PostgreSQL**

При создании или изменении миграций SQL, которые переименовывают таблицы или создают SERIAL поля, ты ОБЯЗАН:

1. **Перед коммитом:**
   - Запустить `scripts/validate-migration.ps1 -MigrationFile "backend/migrations/XXX_migration.sql"`
   - Убедиться, что миграция включает переименование последовательности (если переименовывается таблица)
   - Убедиться, что миграция выдает права доступа на последовательность (если создается SERIAL поле)

2. **При создании миграции:**
   - Использовать шаблон `backend/migrations/TEMPLATE_with_sequence.sql` при переименовании таблиц с SERIAL полями
   - Обязательно добавить переименование последовательности после переименования таблицы
   - Обязательно добавить выдачу прав доступа на последовательность

3. **После применения миграции:**
   - Запустить `scripts/check-sequences-after-migration.ps1 -TableName <table_name>`
   - Проверить, что последовательность имеет правильное имя и права доступа
   - Протестировать сохранение данных в таблицу

4. **При работе с репозиториями:**
   - Использовать `BaseRepository` для всех репозиториев, работающих с таблицами, использующими SERIAL поля
   - Обрабатывать ошибки последовательностей через методы базового класса (`_handle_sequence_error`)

5. **Pre-commit hook:**
   - Pre-commit hook автоматически проверяет миграции перед коммитом
   - Если проверка не прошла - коммит будет отклонен

**Если хотя бы одна проверка не выполнена - задача НЕ считается завершенной!**

**Документация:**
- `docs/TROUBLESHOOTING.md` - раздел "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ"
- `docs/MIGRATION_CHECKLIST.md` - чеклист для миграций
- `docs/FAQ_SEQUENCES.md` - FAQ по работе с последовательностями

## Frontend Rules (Next.js)

- Все HTTP-запросы идут через `frontend/moderator-dashboard-ui/lib/api.ts`.
- Нельзя вызывать `fetch("http://...")` напрямую из компонентов.
- Страницы модератора живут в `app/...` и соответствуют backend endpoint'ам.
- При изменении API:
  - Обновить `lib/api.ts`.
  - Обновить `lib/types.ts`.
  - Обновить соответствующие страницы/компоненты.

**⚠️ КРИТИЧЕСКИ ВАЖНО: Документирование рабочих связок Backend-Frontend**

- **ВСЕ рабочие связки Backend-Frontend ДОЛЖНЫ быть задокументированы**
- **Перед документированием КАЖДЫЙ endpoint, помеченный как рабочий, ДОЛЖЕН быть проверен:**
  1. Проверить, что endpoint работает (запустить и протестировать)
  2. Проверить, что Frontend корректно вызывает endpoint
  3. Проверить, что данные корректно передаются и обрабатываются
  4. Проверить обработку ошибок
- **Документировать:**
  - Как работает связка (какой endpoint, какие данные передаются, как обрабатываются)
  - Команды для проверки работоспособности
  - Ожидаемый результат
  - Примеры запросов/ответов
- **Обновить `docs/PROJECT_SPECIFICATION.md`** с описанием рабочей связки
- **Пометить статус как "✅ Проверено и работает"** только после реальной проверки

## Parser Service & AI

- Parser Service отвечает за:
  - поиск URL (Google/Yandex),
  - работу с Chrome CDP,
  - первичный парсинг HTML.
- Backend взаимодействует с Parser Service через HTTP‑клиент в `app/adapters`.
- Логика AI (Ollama, Qwen) живёт в:
  - `app/adapters/ollama_client.py`
  - `app/services/ai_extractor.py`
- Нельзя:
  - вызывать Chrome CDP напрямую из Backend,
  - дублировать AI‑логику в разных местах.

## Change Workflow

- Делай изменения маленькими порциями: один модуль, один endpoint, одна страница.
- После каждого шага:
  - Backend: убедись, что `uvicorn app.main:app` стартует без ошибок.
  - Frontend: убедись, что `next dev` или `npm run build` проходит.
- Если изменение вызывает 404/500:
  - Не добавляй новый код поверх.
  - Покажи diff и предложи откат/исправление вместо усложнения.

## When Unsure

Если ты не уверен:
- где разместить файл,
- как назвать endpoint,
- как связаны слои,

СНАЧАЛА задай уточняющие вопросы пользователю и НЕ пиши код, основанный на догадках.

## Validation & Done Definition

Перед тем как заявлять, что задача выполнена и решение «готово и работает», ты ОБЯЗАН:

1. Явно перечислить, какие файлы ты изменил и зачем.
2. Описать, КАК проверить решение (конкретные команды и шаги).

### Backend (FastAPI)

Для любых изменений в backend ты ДОЛЖЕН:

- **САМ запустить и проверить, что приложение стартует:**
  - команда: `cd backend` затем `uvicorn app.main:app --reload`
  - проверить, что нет ошибок при старте
- **Если добавлены/изменены эндпоинты:**
  - **САМ протестировать их через `http://127.0.0.1:8000/docs`** (если доступно) или через curl
  - **САМ выполнить пример запроса:**
    - команда: `curl "http://127.0.0.1:8000/..."`
  - проверить ответ и убедиться, что endpoint работает корректно
  - **НЕ просить пользователя протестировать endpoint**

Не пиши, что «всё работает», если:
- ты не запустил и не проверил приложение САМ
- ты не протестировал измененные/добавленные эндпоинты САМ
- потенциально требуются миграции БД, но ты не описал, как их применить.

### Frontend (Next.js)

Для любых изменений во frontend ты ДОЛЖЕН:

- **САМ проверить, что проект собирается:**
  - команда: `cd frontend/moderator-dashboard-ui`
  - `npm run lint` - проверить, что нет ошибок линтера
  - `npm run build` или `npm run dev` - проверить, что проект собирается без ошибок
- **Если изменялся вызов API или страница:**
  - **САМ запустить Frontend и Backend** (если нужно)
  - **САМ открыть страницу в браузере** (через curl или проверить доступность)
  - **САМ проверить, что страница работает** (нет 404/500, данные отображаются корректно)
  - **САМ протестировать функциональность** (отправить запрос, проверить ответ)
  - **САМ проверить результат на Frontend** - открыть страницу, выполнить операцию, убедиться что результат отображается корректно
  - **САМ проверить консоль браузера** (F12) - убедиться, что нет ошибок
  - **САМ проверить Network tab** - убедиться, что запросы успешны
  - **НЕ просить пользователя открыть страницу или протестировать функциональность**

**⚠️ КРИТИЧЕСКИ ВАЖНО: Проверка результатов на Frontend**

После ЛЮБЫХ изменений, затрагивающих Frontend или связку Backend-Frontend, ты ОБЯЗАН:

1. **Запустить необходимые сервисы:**
   - Backend (если изменялся)
   - Frontend (если изменялся)
   - Parser Service (если нужен для тестирования)

2. **Открыть страницу в браузере:**
   - Проверить доступность через `curl http://localhost:3000` или открыть в браузере
   - Убедиться, что страница загружается без ошибок

3. **Выполнить операцию, которая была изменена:**
   - Если изменялось удаление - удалить запись через Frontend
   - Если изменялась фильтрация - применить фильтр через Frontend
   - Если изменялся API вызов - выполнить действие, которое вызывает этот API

4. **Проверить результат:**
   - Убедиться, что результат отображается корректно
   - Убедиться, что состояние обновляется правильно
   - Убедиться, что нет визуальных артефактов или ошибок

5. **Проверить консоль браузера (F12):**
   - Убедиться, что нет ошибок JavaScript
   - Убедиться, что нет ошибок API запросов
   - Проверить логи `console.log` для отладки

6. **Проверить Network tab:**
   - Убедиться, что API запросы успешны (статус 200/204)
   - Убедиться, что ответы корректны
   - Убедиться, что нет ошибок CORS или других проблем

7. **Проверить логи Backend:**
   - Убедиться, что запросы доходят до Backend
   - Убедиться, что нет ошибок в логах
   - Убедиться, что операции выполняются корректно

**Только после выполнения ВСЕХ этих проверок можно заявлять, что задача завершена!**

Нельзя писать «готово», пока ты не:
- запустил проверку сборки САМ
- протестировал изменения через фронтенд САМ
- проверил результат на Frontend САМ (открыл страницу, выполнил операцию, убедился что работает)
- проверил консоль браузера САМ
- проверил Network tab САМ
- убедился, что все работает корректно

### Parser Service / AI

Если ты менял Parser Service или AI-интеграцию:

- Приведи команды для запуска всех сервисов в правильном порядке:
  1. Chrome CDP
  2. Parser Service
  3. Backend
  4. Frontend
- Приведи пример запроса, который демонстрирует работу:
  - пример `POST /parse` или `POST /ai/extract-from-url` с телом и ожидаемым ответом.

### Общий критерий "Задача завершена"

Задача считается завершённой, только если ты:

1. Описал все изменения.
2. **Проверил импорты** (для Backend):
   - Если изменялись роутеры или usecases — запустил `python temp/backend/check_imports.py`
   - Убедился, что все используемые типы (`date`, `datetime` и т.д.) имеют соответствующие импорты
   - Если есть ошибки — исправил их ПЕРЕД завершением задачи
3. **Проверил работоспособность (ОБЯЗАТЕЛЬНО САМ через терминал):**
   - **САМ запустил код и убедился, что он работает** (через терминал, не прося пользователя)
   - **САМ проверил, что исправление решает проблему** (протестировал функциональность)
   - **САМ проверил, что ничего не сломалось** (запустил существующие тесты, проверил другие функции)
   - **САМ протестировал через фронтенд** (если изменения затрагивают Frontend - запустил сервисы, открыл страницы, проверил работу)
   - **НЕ просил пользователя выполнить команды или протестировать изменения**
4. **Обновил документацию:**
   - Добавил ошибку в `docs/TROUBLESHOOTING.md` (если новая)
   - Обновил `docs/PROJECT_SPECIFICATION.md` ТОЛЬКО если исправление проверено и работает
   - Изменил статус с "⚠️ Требует проверки" на "✅ Проверено и работает" только после реальной проверки
   - **ОБЯЗАТЕЛЬНО документировал все рабочие связки Backend-Frontend:**
     - Перед документированием КАЖДЫЙ endpoint, помеченный как рабочий, должен быть проверен
     - Документировать, как работает связка (какой endpoint, какие данные передаются, как обрабатываются)
     - Документировать проверку работоспособности (команды, ожидаемый результат)
     - Обновить `docs/PROJECT_SPECIFICATION.md` с описанием рабочей связки
5. **Дал рекомендации для повышения эффективности:**
   - ОБЯЗАТЕЛЬНО после каждого исправления
   - Конкретные и практичные рекомендации
   - С приоритетами и примерами
   - Проверил, что рекомендации не засоряют структуру проекта
6. Привёл точные команды для проверки (backend, frontend, parser/AI — по необходимости).
7. Описал, какой результат должен увидеть пользователь (URL, статус, важные поля ответа).
8. Указал возможные ограничения (что ещё НЕ сделано или требует дальнейшей настройки).

Если какой‑то из этих пунктов не выполнен — не говори, что задача закрыта, а явно напиши, что решение ТРЕБУЕТ проверки пользователем.

**⚠️ КРИТИЧЕСКИ ВАЖНО: Обновление спецификации**

- **НЕ обновляй** `docs/PROJECT_SPECIFICATION.md` на основе предположений
- **НЕ меняй** статус с "⚠️ Требует проверки" на "✅ Проверено" без реальной проверки
- **ОБЯЗАТЕЛЬНО** обновляй спецификацию после успешной проверки работоспособности
- **Помечай** компоненты как "❌ Не работает" если есть известные проблемы

**⚠️ КРИТИЧЕСКИ ВАЖНО: Проверка импортов**

После ЛЮБОГО изменения файлов роутеров или usecases ОБЯЗАТЕЛЬНО:
1. Запустить `python temp/backend/check_imports.py`
2. Если есть ошибки — исправить их
3. Только после этого заявлять, что задача завершена

Это предотвращает повторение ошибок типа "NameError: name 'date' is not defined", которые возникают при:
- Перезаписи файлов
- Откате изменений
- Копировании кода без импортов
- Рефакторинге с удалением импортов

## Изменение существующих файлов (Safe Editing)

При работе с ВАЖНЫМИ файлами (роуты, usecases, adapters, db‑модели, api.ts, types.ts) действую ТАК:

1. Сначала ЧИТАЮ файл и КРАТКО формулирую, что он делает.
2. **Проверяю все импорты** — убеждаюсь, что все используемые типы и классы импортированы.
3. В ответе описываю ПЛАН изменений (списком), не меняя код.
4. ЖДУ подтверждения пользователя на план, если изменения затрагивают:
   - структуру БД,
   - существующие API‑эндпоинты,
   - общие типы DTO,
   - ключевые компоненты фронта.

**После внесения изменений:**
- Проверяю, что все необходимые импорты присутствуют
- Запускаю `python temp/backend/check_imports.py` (для Backend)
- Убеждаюсь, что код компилируется без ошибок

Запрещено:
- Переписывать файл «целиком с нуля», если можно внести локальные изменения.
- Удалять блоки кода, если не уверен, что они не используются.
- Переименовывать функции/классы/поля, не проверив все места использования.
- **Удалять или не добавлять необходимые импорты** при редактировании файла.

## Правила работы с импортами (критически важно!)

**⚠️ Эти правила предотвращают ошибки типа "NameError: name 'date' is not defined"**

### При редактировании файлов

1. **Перед началом редактирования:**
   - Проверяю, что все используемые типы и классы импортированы
   - Если вижу использование типа без импорта — добавляю импорт СРАЗУ

2. **При копировании кода:**
   - **ОБЯЗАТЕЛЬНО копирую блок импортов вместе с кодом**
   - Если копирую функцию, использующую `date` — копирую и `from datetime import date`
   - Если копирую из документации — проверяю, что все импорты из примеров добавлены

3. **При рефакторинге:**
   - **НЕ удаляю импорты**, которые используются в коде
   - Перед удалением импорта проверяю, что он нигде не используется (grep по файлу)
   - При реорганизации импортов сохраняю все необходимые

4. **При откате изменений:**
   - Проверяю, что критически важные импорты не удалены
   - После отката запускаю `python temp/backend/check_imports.py`
   - Если импорты отсутствуют — добавляю их обратно

### Перед завершением задачи

**ОБЯЗАТЕЛЬНО:**
1. Запустить `python temp/backend/check_imports.py`
2. Если есть ошибки — исправить их ПЕРЕД завершением задачи
3. Убедиться, что код компилируется без ошибок

**Нельзя заявлять, что задача завершена, если:**
- Скрипт проверки импортов не был запущен
- Есть ошибки импортов, которые не исправлены
- Код не компилируется из-за отсутствующих импортов

### Перед коммитом (рекомендация для пользователя)

Рекомендуется добавить в pre-commit hook:
```bash
python temp/backend/check_imports.py
exit $?
```

Но даже без hook, перед коммитом ОБЯЗАТЕЛЬНО:
1. Запустить `python temp/backend/check_imports.py`
2. Убедиться, что код компилируется без ошибок
3. Проверить, что все используемые типы импортированы

## Экспериментальные изменения через копию

Если предложенное решение НЕ очевидно рабочее (нужно проверить гипотезу, есть риск сломать существующий сценарий), то:

1. НЕ изменяю оригинальный файл напрямую.
2. Делаю копию РЯДОМ, в temp/ или experiments/, с понятным именем:

   - `temp/backend/experiments/new_parsing_flow.py`
   - `temp/frontend/experiments/new_supplier_detail.tsx`

3. В ответе явно пишу:
   - какой файл является ОРИГИНАЛОМ,
   - где лежит ЭКСПЕРИМЕНТАЛЬНАЯ версия,
   - как её можно подключить/протестировать.

Только после того, как пользователь подтвердит, что экспериментальная версия работает, можно:
- аккуратно перенести изменения в основной файл,
- удалить временный эксперимент.

## Критические точки — трогаю только по прямому запросу

К критически важным файлам относятся:

- `backend/app/transport/routers/*` — существующие эндпоинты
- `backend/app/usecases/*` — ключевая бизнес-логика
- `backend/app/adapters/db/models.py` и миграции
- `frontend/moderator-dashboard-ui/lib/api.ts`
- `frontend/moderator-dashboard-ui/lib/types.ts`

Правило:
- НЕ менять сигнатуры, пути, названия полей и типов в этих файлах,
  если пользователь явно не просил именно это.
- Если для задачи всё-таки нужно изменить критический файл —
  сначала описать, ЧТО именно изменится и КАКИЕ части приложения это затронет.

## Сохранение старой реализации

Если нужно значительно изменить функцию/класс/компонент:

1. НЕ переписываю существующую реализацию поверх.
2. Сохраняю старую версию под новым именем, например:
   - `old_get_parsing_status`
   - `SupplierDetailLegacy`
3. Новую версию пишу РЯДОМ, с новым именем.
4. В комментарии кратко объясняю:
   - чем новая версия отличается,
   - как при необходимости вернуть старую.

Пользователь сам решает, когда удалить legacy‑реализацию.

## Напоминание про безопасность изменений

Перед тем как утверждать, что «решение готово и работает», я:

- НЕ удаляю/не ломаю старый код без бэкапа или альтернативы.
- Даю команды для запуска и проверки (backend/frontend/parser).
- Ясно описываю, что именно нужно проверить вручную.
- Не говорю, что всё работает, пока не проверено согласно разделу "Validation & Done Definition".

## Рекомендации для повышения эффективности

**⚠️ КРИТИЧЕСКИ ВАЖНО:** После КАЖДОГО решения задачи или исправления ошибки я ОБЯЗАТЕЛЬНО ДОЛЖЕН дать рекомендации для повышения эффективности работы.

**Это правило применяется ВСЕГДА, без исключений.**

**Что включать в рекомендации:**

1. **Профилактика ошибок:**
   - Как избежать повторения этой ошибки
   - Какие проверки добавить
   - Какие инструменты использовать

2. **Улучшение процесса разработки:**
   - Автоматизация повторяющихся задач
   - Улучшение тестирования
   - Оптимизация workflow

3. **Оптимизация кода:**
   - Рефакторинг для улучшения читаемости
   - Улучшение производительности
   - Улучшение архитектуры

4. **Улучшение документации:**
   - Что нужно задокументировать
   - Какие примеры добавить
   - Какие чеклисты создать

5. **Инструменты и автоматизация:**
   - Скрипты для проверки
   - Pre-commit hooks
   - CI/CD улучшения

**Формат рекомендаций:**
- Краткие и конкретные
- С примерами кода или команд
- С приоритетами (что важнее сделать сначала)
- С ссылками на документацию

**Пример:**
```markdown
## 💡 Рекомендации для повышения эффективности

1. **Добавить автоматическую проверку импортов в pre-commit hook:**
   ```bash
   # В .git/hooks/pre-commit
   python temp/backend/check_imports.py
   exit $?
   ```
   Это предотвратит коммиты с отсутствующими импортами.

2. **Создать тесты для валидации параметров:**
   - Unit тесты для проверки валидации ID
   - Integration тесты для проверки endpoints
   ...
```

## История действий и отсутствие повторов

Для КАЖДОЙ задачи я обязан вести mini‑лог своих действий и ссылаться на него, чтобы не повторять одно и то же.

### Что я фиксирую

В конце каждого большого шага (ответа) я кратко перечисляю:

1. **Что было сделано**
   - изменённые файлы (с путями),
   - добавленные/удалённые файлы,
   - ключевые функции/эндпоинты, которые трогал.

2. **Что НЕ сработало**
   - какие подходы/гипотезы пробовал и ОТКАЗАЛСЯ использовать,
   - какие идеи показали себя нерабочими и почему.

3. **Статус задачи**
   - что уже решено,
   - что ещё осталось сделать,
   - какие риски/неясности остались.

Пример формата:

- Сделано:
  - `backend/app/transport/routers/parsing.py`: добавлен endpoint `GET /parsing-runs/{run_id}`.
  - `frontend/.../lib/api.ts`: добавлен метод `getParsingRun(runId)`.
- Не сработало:
  - Пытался получить данные напрямую из Parser Service — отказался, оставил через Backend.
- Осталось:
  - Добавить UI для отображения ошибок.

### Правило против повторов

Перед тем как предлагать новый план или менять код, я ОБЯЗАН:

1. Просмотреть свой предыдущий mini‑лог по этой задаче.
2. НЕ предлагать снова:
   - уже отвергнутые подходы,
   - уже сделанные, но не проверенные изменения.
3. Если повтор всё‑таки нужен (например, пользователь попросил вернуться к старой идее) —
   явно указать: «Это повтор предыдущего шага по просьбе пользователя».

Запрещено:

- Предлагать один и тот же рефакторинг/подход несколько раз подряд, не ссылаясь на предыдущие попытки.
- «Забывать», какие файлы уже менялись в рамках текущей задачи.

## Использование терминала

**⚠️ КРИТИЧЕСКИ ВАЖНО: Ты ОБЯЗАН выполнять все проверки и тесты самостоятельно через терминал!**

У тебя есть доступ к встроенному терминалу Cursor Agent.

**ОБЯЗАТЕЛЬНО:**

- **Всегда, когда для решения задачи можно что‑то проверить или выполнить через терминал**
  (запуск сервиса, тесты, curl‑запрос, проверка порта, просмотр логов и т.п.),
  **ВЫПОЛНЯЙ эти команды САМ через терминал, а не проси пользователя сделать это.**

- **После ЛЮБЫХ изменений в коде ОБЯЗАТЕЛЬНО:**
  1. Запустить все необходимые скрипты проверки через свое окружение (терминал)
  2. Убедиться, что код компилируется без ошибок
  3. Убедиться, что изменения работают корректно
  4. Протестировать через фронтенд (открыть страницы, проверить функциональность)
  5. Только после этого заявлять, что задача завершена

- **При тестировании Frontend:**
  - Запустить сервисы (Backend, Parser Service, Frontend) через терминал
  - Открыть страницы в браузере через терминал (если возможно) или проверить через API
  - Проверить, что изменения работают через фронтенд
  - **НЕ просить пользователя выполнить команды или открыть страницы**

- **При проверке изменений:**
  - Запустить скрипты проверки (например, `python temp/backend/check_imports.py`)
  - Запустить тесты (если есть)
  - Проверить работоспособность через API запросы
  - Проверить логи на наличие ошибок
  - **Все проверки выполнять САМОСТОЯТЕЛЬНО через терминал**

- В ответе показывай:
  - какие команды были запущены;
  - кратко к чему они привели (успех / ошибка, важные строки вывода);
  - результаты тестирования через фронтенд (если применимо).

**ЗАПРЕЩЕНО:**

- Просить пользователя выполнить команды для проверки изменений
- Просить пользователя открыть страницы фронтенда для тестирования
- Заявлять, что «всё готово» или «должно работать», если ты мог выполнить проверку через терминал, но не сделал этого
- Завершать задачу без проверки работоспособности через терминал и фронтенд

## Мониторинг состояния системы

**⚠️ КРИТИЧЕСКИ ВАЖНО: При диагностике проблем ОБЯЗАТЕЛЬНО проверяй состояние системы!**

### Использование скриптов мониторинга

При диагностике проблем, ошибок или неработающих сервисов ОБЯЗАТЕЛЬНО:

1. **Проверяй состояние всех сервисов через скрипты мониторинга:**
   - Используй `scripts/monitor-services.ps1` для проверки состояния всех сервисов
   - Или запускай отдельные проверки через терминал:
     ```powershell
     # Проверка Chrome CDP
     netstat -ano | findstr ":9222"
     curl http://127.0.0.1:9222/json/version
     
     # Проверка Parser Service
     curl http://127.0.0.1:9003/health
     
     # Проверка Backend
     curl http://127.0.0.1:8000/health
     
     # Проверка Frontend
     curl http://localhost:3000
     ```

2. **Читай логи сервисов для диагностики:**
   - Логи находятся в `logs/` директории
   - Используй `Get-Content` для чтения последних строк логов:
     ```powershell
     Get-Content logs/Backend-*.log -Tail 20
     Get-Content logs/Parser\ Service-*.log -Tail 20
     Get-Content logs/Frontend-*.log -Tail 20
     ```

3. **Проверяй процессы и порты:**
   - Проверяй, что процессы запущены: `Get-Process | Where-Object {$_.ProcessName -like "*python*" -or $_.ProcessName -like "*node*"}`
   - Проверяй, что порты слушаются: `netstat -ano | findstr ":8000 :9003 :3000 :9222"`

4. **Используй Main Monitor для комплексной диагностики:**
   - Скрипт `logs/main-monitor.ps1` запускает полный мониторинг всех сервисов
   - При серьезных проблемах запускай одноразовую проверку:
     ```powershell
     powershell.exe -ExecutionPolicy Bypass -File 'scripts\monitor-services.ps1' -ProjectRoot 'D:\tryagain'
     ```

### Порядок диагностики

**⚠️ КРИТИЧЕСКИ ВАЖНО: При диагностике ЛЮБОЙ проблемы ты ОБЯЗАН следовать этому порядку!**

При получении ошибки или проблемы:

1. **СНАЧАЛА проверь состояние системы (ОБЯЗАТЕЛЬНО!):**
   - **Используй чеклист из `docs/TROUBLESHOOTING.md` раздел "Рекомендации для быстрой диагностики проблем" → "1. Проверка состояния системы"**
   - Выполни ВСЕ команды из чеклиста:
     ```powershell
     # 1. Проверка портов и процессов
     netstat -ano | findstr ":8000 :9003 :3000 :9222"
     Get-Process | Where-Object {$_.ProcessName -like "*python*" -or $_.ProcessName -like "*node*"}
     
     # 2. Проверка health endpoints
     curl http://127.0.0.1:8000/health
     curl http://127.0.0.1:9003/health
     curl http://localhost:3000
     
     # 3. Проверка Chrome CDP
     curl http://127.0.0.1:9222/json/version
     
     # 4. Проверка последних ошибок в логах
     Get-Content "logs\Backend-*.log" -Tail 50 | Select-String -Pattern "ERROR|Exception|Traceback"
     Get-Content "logs\Parser Service-*.log" -Tail 50 | Select-String -Pattern "ERROR|Exception|Traceback"
     ```
   - Запусти проверку всех сервисов через скрипты мониторинга (если нужно)
   - Проверь логи последних ошибок
   - Убедись, что все необходимые сервисы запущены
   - **НЕ предлагай решения, пока не выполнил эти проверки!**

2. **ЗАТЕМ ищи решение:**
   - Проверь `docs/TROUBLESHOOTING.md` на наличие похожих проблем (раздел "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ" и все ошибки)
   - Используй раздел "Рекомендации для быстрой диагностики проблем" из `docs/TROUBLESHOOTING.md`
   - Изучи логи для понимания причины ошибки
   - Используй информацию о состоянии системы для точной диагностики

3. **ПОСЛЕ исправления:**
   - Проверь, что все сервисы работают корректно (повтори проверки из пункта 1)
   - Убедись, что проблема решена через повторную проверку состояния
   - Задокументируй решение в `docs/TROUBLESHOOTING.md`

### Что проверять при диагностике

**Обязательные проверки:**
- ✅ Chrome CDP доступен на порту 9222 (видимый режим, не headless)
- ✅ Parser Service отвечает на `http://127.0.0.1:9003/health`
- ✅ Backend отвечает на `http://127.0.0.1:8000/health`
- ✅ Frontend доступен на `http://localhost:3000`
- ✅ База данных доступна (проверка подключения)
- ✅ Нет ошибок в логах сервисов

**Дополнительные проверки при специфических проблемах:**
- Проверка миграций БД (если проблема связана с БД)
- Проверка конфигурации (`.env` файлы)
- Проверка зависимостей (установлены ли все пакеты)
- Проверка версий Python/Node.js

### Инструменты мониторинга

**Основные скрипты:**
- `logs/main-monitor.ps1` - главный скрипт мониторинга (запускает `scripts/monitor-services.ps1`)
- `scripts/monitor-services.ps1` - скрипт проверки состояния всех сервисов
- `scripts/check_chrome_cdp.bat` - проверка Chrome CDP
- `scripts/capture-service-logs.ps1` - захват логов сервисов

**Логи сервисов:**
- `logs/Backend-*.log` - логи Backend
- `logs/Parser Service-*.log` - логи Parser Service
- `logs/Frontend-*.log` - логи Frontend

**Важно:**
- **ОБЯЗАТЕЛЬНО проверяй состояние системы ПЕРЕД предложением решения** - используй чеклист из `docs/TROUBLESHOOTING.md` раздел "Рекомендации для быстрой диагностики проблем" → "1. Проверка состояния системы"
- Используй информацию о состоянии для точной диагностики
- **НЕ предлагай решения, не проверив, что сервисы запущены и работают**
- **Следуй чеклисту диагностики из `docs/TROUBLESHOOTING.md` раздел "7. Чеклист диагностики"** - проверь все 8 пунктов по порядку перед предложением решения

## Git и страховка перед крупными изменениями

Перед любыми крупными изменениями (рефакторинг модулей, изменение схемы БД, массовые правки фронта):

- Убедись, что рабочее состояние закоммичено в Git.
- Если в проекте ещё нет Git, предложи инициализировать его и настроить .gitignore.
- Не выполняй опасные изменения, если в дереве есть незакоммиченные файлы, о которых пользователь ничего не говорил.

## Документирование ошибок и решений (БИБЛИЯ)

**⚠️ КРИТИЧЕСКИ ВАЖНО: Все ошибки и их решения ОБЯЗАТЕЛЬНО документируются и являются "НЕПРИКОСНОВЕННЫМИ, КАК БИБЛИЯ".**

### Правила документирования:

1. **Каждая ошибка ДОЛЖНА быть задокументирована:**
   - В файле `docs/TROUBLESHOOTING.md` (основной документ "Библия ошибок")
   - С полным описанием проблемы, причины, решения и проверки
   - С указанием даты решения и измененных файлов

2. **Решения НЕ ДОЛЖНЫ быть удалены:**
   - Даже если кажется, что проблема больше не актуальна
   - Даже если код изменился и решение выглядит устаревшим
   - Решения могут быть помечены как "устаревшие", но НЕ удалены

3. **Перед решением новой ошибки:**
   - ОБЯЗАТЕЛЬНО проверь `docs/TROUBLESHOOTING.md`
   - Если похожая ошибка уже была решена - используй существующее решение
   - Если решение не подходит - документируй новое решение рядом со старым

4. **Формат документации ошибки:**
   ```
   ## Ошибка N: [Название]
   
   ### Описание проблемы
   - Что происходит
   - Какие ошибки видны
   - Где возникает
   
   ### Причина
   - Почему это происходит
   - Что вызывает проблему
   
   ### Решение
   - Пошаговое решение
   - Код изменений
   - Команды для применения
   
   ### Проверка
   - Команды для проверки
   - Ожидаемый результат
   
   ### Измененные файлы
   - Список файлов с изменениями
   
   ### Дата решения
   YYYY-MM-DD
   ```

5. **При изменении кода, связанного с задокументированной ошибкой:**
   - Проверь, не ломает ли изменение существующее решение
   - Если решение нужно обновить - обнови документацию, но сохрани старую версию как "Устаревшее решение"

6. **Запрещено:**
   - Удалять разделы с решениями ошибок
   - Изменять решения без обновления документации
   - Игнорировать существующие решения при повторении ошибки

