# Rule Type: Always
# B2B Platform - Project Rules for Cursor

## 🚨🚨🚨 САМОЕ ГЛАВНОЕ ПРАВИЛО - НЕПРИКОСНОВЕННО! 🚨🚨🚨

**⚠️⚠️⚠️ КРИТИЧЕСКИ ВАЖНО: ПОДТВЕРЖДЕНИЕ ПОНИМАНИЯ ПОСЛЕ КАЖДОГО СООБЩЕНИЯ ПОЛЬЗОВАТЕЛЯ ⚠️⚠️⚠️**

**После КАЖДОГО сообщения пользователя ты ОБЯЗАН:**

1. **ОБЯЗАТЕЛЬНО подтвердить понимание задачи:**
   - Начать ответ с фразы типа: "Я правильно понял? Вы хотите, чтобы..."
   - Описать своими словами, что именно нужно сделать
   - Уточнить детали, если что-то неясно
   - Только после подтверждения приступать к реализации

**Пример:**
- "Я правильно понял? Вы хотите, чтобы при открытии страницы parsing-runs автоматически запускалось извлечение ИНН для всех доменов, и найденные ИНН отображались желтым цветом с ссылкой на пруф?"

**ЗАПРЕЩЕНО:**
- Начинать реализацию без подтверждения понимания
- Предполагать, что понял задачу, не уточнив детали
- Пропускать этап подтверждения понимания

---

**⚠️⚠️⚠️ КРИТИЧЕСКИ ВАЖНО: ПРОВЕРКА НА ФРОНТЕНДЕ ПОСЛЕ КАЖДОЙ ЗАДАЧИ ⚠️⚠️⚠️**

**ЭТО MUST HAVE! ЭТО СВЯТОЕ ПРАВИЛО, КОТОРОЕ НЕЛЬЗЯ НАРУШАТЬ!**

**После КАЖДОЙ выполненной задачи, которая затрагивает Frontend или Backend API, ты ОБЯЗАН:**

1. **ОБЯЗАТЕЛЬНО запустить Frontend** (если не запущен):
   ```powershell
   cd frontend/moderator-dashboard-ui
   npm run dev
   ```

2. **ОБЯЗАТЕЛЬНО открыть изменённую страницу в браузере:**
   - Открыть `http://localhost:3000` и перейти на изменённую страницу
   - Выполнить действия, которые используют изменённую функциональность
   - **Убедиться, что результат запускается и отображается так, как это задумано (И ОЖИДАЕТСЯ)**

3. **ОБЯЗАТЕЛЬНО проверить:**
   - Консоль браузера (F12) - не должно быть ошибок
   - Network tab - запросы должны быть успешными
   - UI работает корректно и отображает ожидаемый результат

**ЗАПРЕЩЕНО:**
- Заявлять, что задача завершена, не проверив на фронтенде
- Говорить "должно работать" или "логика правильная" без реальной проверки
- Пропускать проверку на фронтенде, даже если изменения кажутся "незначительными"
- Просить пользователя протестировать изменения вместо самостоятельной проверки

**ЭТО ПРАВИЛО НЕ МОЖЕТ БЫТЬ ИГНОРИРОВАНО ИЛИ ПРОПУЩЕНО!**

---

## 🚨 ВАЖНО: ВХОДНАЯ ТОЧКА ДЛЯ НОВОГО ЧАТА

**Перед началом работы в новом чате ОБЯЗАТЕЛЬНО:**

1. **Прочитай `docs/MASTER_INSTRUCTION.md`** - главная инструкция проекта (запуск, настройка, архитектура)
2. **Прочитай `docs/TROUBLESHOOTING.md`** - библия ошибок и решений
   - **ОБЯЗАТЕЛЬНО изучи раздел "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ"** - первое, что нужно делать при диагностике
   - **ОБЯЗАТЕЛЬНО изучи раздел "Чеклист диагностики"** - проверь все 8 пунктов
3. **Прочитай `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md`** - критические точки интеграции и чеклисты
   - **ОБЯЗАТЕЛЬНО изучи перед изменением любой функциональности, которая используется в нескольких местах**

**Документация проекта:**
- **Главная инструкция**: `docs/MASTER_INSTRUCTION.md` - **НАЧНИ ОТСЮДА!**
- **Библия ошибок**: `docs/TROUBLESHOOTING.md` - все ошибки и решения
- **Критические точки**: `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md` - места использования функциональности и чеклисты
- **Спецификация API**: `docs/PROJECT_SPECIFICATION.md` - детальная спецификация API
- **Карта проекта**: `docs/PROJECT_MAP.md` - структура проекта

**При возникновении ошибки:**
- **СНАЧАЛА выполни проверки из `docs/TROUBLESHOOTING.md` раздел "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ"**
- **СНАЧАЛА используй чеклист из `docs/TROUBLESHOOTING.md` раздел "Чеклист диагностики"**
- Затем проверь `docs/TROUBLESHOOTING.md` на наличие похожих проблем
- Если решения нет - реши и ОБЯЗАТЕЛЬНО задокументируй в `docs/TROUBLESHOOTING.md`

## Overview

- Purpose: B2B-платформа с личным кабинетом модератора, парсингом сайтов поставщиков и интеграцией с Checko.
- Stack:
  - Backend: FastAPI, PostgreSQL, SQLAlchemy, Pydantic v2
  - Frontend: Next.js (App Router), TypeScript, Tailwind, Shadcn UI
  - Parser Service: Python, Chrome CDP (Playwright)
- Key docs: `docs/MASTER_INSTRUCTION.md`, `docs/TROUBLESHOOTING.md`, `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md`

## ⚠️ ВАЖНО: Организация файлов

**ЗАПРЕЩЕНО:**
- Создавать временные файлы и скрипты в корне проекта
- Создавать тестовые файлы для проверки гипотез в основных директориях

**ОБЯЗАТЕЛЬНО:**
- Все временные скрипты, проверки гипотез, тестовые файлы создавать в отдельной папке `temp/` или `experiments/`
- После завершения работы с временными файлами - удалять их или перемещать в архив

## Architecture Principles

- Три отдельных сервиса:
  - `backend/` — API и бизнес-логика
  - `frontend/moderator-dashboard-ui/` — личный кабинет модератора
  - `parser_service/` — парсинг через Chrome CDP
- Не перемешивать ответственность между сервисами
- Сначала описывать/уточнять архитектурные изменения текстом, потом менять код

## Backend Rules (FastAPI)

- Основные директории:
  - `app/transport/routers` — только FastAPI endpoints
  - `app/usecases` — бизнес-логика
  - `app/adapters` — внешние сервисы (Parser Service, Checko, Ollama)
  - `app/db` — модели и работа с БД
- Запрещено:
  - Писать SQL и сложную логику прямо в роутерах
  - Менять структуру таблиц без миграций
  - Переименовывать поля DTO без обновления Pydantic-схем и TS-типов фронта

**⚠️ КРИТИЧЕСКИ ВАЖНО: Обязательные проверки миграций с последовательностями PostgreSQL**

При создании или изменении миграций SQL, которые переименовывают таблицы или создают SERIAL поля, ты ОБЯЗАН:

1. **Перед коммитом:** Запустить `scripts/validate-migration.ps1 -MigrationFile "backend/migrations/XXX_migration.sql"`
2. **При создании миграции:** Использовать шаблон `backend/migrations/TEMPLATE_with_sequence.sql`
3. **После применения миграции:** Запустить `scripts/check-sequences-after-migration.ps1 -TableName <table_name>`
4. **При работе с репозиториями:** Использовать `BaseRepository` для всех репозиториев, работающих с таблицами, использующими SERIAL поля

**Если хотя бы одна проверка не выполнена - задача НЕ считается завершенной!**

**Документация:** `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md` - раздел "Часть 3: Чеклисты для миграций"

## Frontend Rules (Next.js)

- Все HTTP-запросы идут через `frontend/moderator-dashboard-ui/lib/api.ts`
- Нельзя вызывать `fetch("http://...")` напрямую из компонентов
- Страницы модератора живут в `app/...` и соответствуют backend endpoint'ам
- При изменении API:
  - Обновить `lib/api.ts`
  - Обновить `lib/types.ts`
  - Обновить соответствующие страницы/компоненты

## Parser Service & AI

- Parser Service отвечает за:
  - поиск URL (Google/Yandex)
  - работу с Chrome CDP
  - первичный парсинг HTML
- Backend взаимодействует с Parser Service через HTTP‑клиент в `app/adapters`
- Нельзя:
  - вызывать Chrome CDP напрямую из Backend
  - дублировать AI‑логику в разных местах

## Change Workflow

**⚠️ КРИТИЧЕСКИ ВАЖНО: Перед изменением любого файла ОБЯЗАТЕЛЬНО выполнить анализ влияния!**

**Перед изменением:**

1. **Проверить состояние системы:**
   ```powershell
   .\scripts\quick-check.ps1 all
   ```

2. **Найти все места использования:**
   ```powershell
   grep -r "functionName\|TypeName\|endpointName" frontend/ backend/
   codebase_search "Where is X used?"
   ```

3. **Проверить паттерны проекта (ОБЯЗАТЕЛЬНО!):**
   - **ОБЯЗАТЕЛЬНО прочитать `docs/PROJECT_PATTERNS.md`** на наличие паттерна
   - **ОБЯЗАТЕЛЬНО найти аналогичные места в проекте:**
     ```powershell
     codebase_search "Where is ComponentName used with SimilarComponent?"
     grep -r "SimilarPattern" frontend/ backend/
     ```
   - **ОБЯЗАТЕЛЬНО изучить правильные паттерны** в других файлах проекта
   - **ОБЯЗАТЕЛЬНО проверить комментарии в коде** на наличие важных решений (например, "вынесено для исправления", "fix", "workaround")

4. **Проверить структуру компонента (если изменяется Frontend):**
   - Нет ли проблемных паттернов (button в button, a в a)?
   - Соответствует ли структура паттернам из `docs/PROJECT_PATTERNS.md`?
   - Если структура проблемная - **ИСПРАВИТЬ СРАЗУ, ПЕРЕД добавлением функционала**

5. **Прочитать документацию:**
   - `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md` (если функциональность используется в нескольких местах)
   - `docs/PROJECT_MAP.md` (для понимания структуры)
   - `docs/PROJECT_PATTERNS.md` (для понимания правильных паттернов)

6. **Проанализировать влияние:**
   - Какие файлы будут затронуты
   - Какие функциональности могут сломаться
   - Предупредить о потенциальных проблемах

**Во время изменения:**

- Делай изменения маленькими порциями: один модуль, один endpoint, одна страница
- После каждого шага:
  - Backend: убедись, что `uvicorn app.main:app` стартует без ошибок
  - Frontend: убедись, что `next dev` или `npm run build` проходит

**После изменения:**

1. **Запустить проверки:**
   ```powershell
   .\scripts\quick-check.ps1 all
   python temp/backend/check_imports.py  # для Backend
   cd frontend/moderator-dashboard-ui && npm run lint  # для Frontend
   ```

2. **ОБЯЗАТЕЛЬНОЕ ТЕСТИРОВАНИЕ (см. раздел "Validation & Done Definition"):**
   - **Если изменены скрипты запуска:** запустить сервисы и проверить health endpoints
   - **Если изменён Backend:** запустить Backend, проверить через API и Frontend
   - **Если изменён Frontend:** запустить Frontend, открыть в браузере, проверить функциональность
   - **Если изменена структура БД:** применить миграцию, проверить работу с БД

3. **Обновить документацию** (если нужно)

## Validation & Done Definition

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ЗАДАЧА ВЫПОЛНЕНА ТОЛЬКО ПОСЛЕ ПРОВЕРКИ ЧЕРЕЗ ФРОНТЕНД! 🚨🚨🚨**

**⚠️⚠️⚠️ ЭТО БЛОКИРУЮЩЕЕ ПРАВИЛО - БЕЗ ПРОВЕРКИ ЧЕРЕЗ ФРОНТЕНД ЗАДАЧА НЕ СЧИТАЕТСЯ ВЫПОЛНЕННОЙ! ⚠️⚠️⚠️**

**ЗАДАЧА СЧИТАЕТСЯ ВЫПОЛНЕННОЙ ТОЛЬКО КОГДА:**

1. ✅ Ты полностью выполнил use-case через фронтенд (открыл страницу, выполнил действия)
2. ✅ Ты получил результат через фронтенд (увидел ожидаемый результат на экране)
3. ✅ Ты убедился, что задача выполнена, потому что есть подтверждение (результат работает как задумано)

**ЗАПРЕЩЕНО заявлять о завершении задачи, если:**
- ❌ Не открыл страницу в браузере
- ❌ Не выполнил действия, которые используют изменённую функциональность
- ❌ Не увидел результат на экране
- ❌ Не проверил, что результат работает как задумано

**ФОРМУЛИРОВКИ, КОТОРЫЕ ЗАПРЕЩЕНЫ БЕЗ ПРОВЕРКИ:**
- "Задача выполнена" → использовать только после проверки через фронтенд
- "Готово" → использовать только после проверки через фронтенд
- "Исправлено" → использовать только после проверки через фронтенд
- "Должно работать" → ЗАПРЕЩЕНО! Использовать "Проверено и работает" после проверки

**ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ ПЕРЕД ЗАЯВЛЕНИЕМ О ЗАВЕРШЕНИИ:**

Для задач, которые затрагивают Frontend или Backend API:

- [ ] Запустил Frontend (если не запущен)
- [ ] Открыл изменённую страницу в браузере (`http://localhost:3000/...`)
- [ ] Выполнил действия, которые используют изменённую функциональность (клик, ввод, выбор и т.д.)
- [ ] Увидел результат на экране (не в логах, не в консоли, а на экране браузера)
- [ ] Убедился, что результат работает как задумано (соответствует ожиданиям)
- [ ] Проверил консоль браузера (F12):
  - Нет ошибок JavaScript
  - Нет ошибок гидратации React
  - Нет предупреждений о deprecated API
- [ ] Проверил Network tab:
  - Все запросы успешны (200, 201, 204)
  - Нет failed запросов
  - Время ответа приемлемое
- [ ] Проверил структуру HTML:
  - Нет вложенных кнопок (button внутри button)
  - Нет вложенных ссылок (a внутри a)
  - Семантическая структура HTML корректна
- [ ] Проверил функциональность:
  - Все интерактивные элементы работают
  - Состояния обновляются корректно
  - Нет визуальных глюков
- [ ] Могу описать, что именно я увидел на экране и как это подтверждает выполнение задачи

**ТОЛЬКО ПОСЛЕ ВЫПОЛНЕНИЯ ВСЕХ ПУНКТОВ ЧЕКЛИСТА можно заявить:**
- "Задача выполнена. Проверено через фронтенд: [описание того, что увидел]"

**⚠️⚠️⚠️ КРИТИЧЕСКИ ВАЖНО: ПРОВЕРКА НА ФРОНТЕНДЕ - ЭТО MUST HAVE! ⚠️⚠️⚠️**

**⚠️ КРИТИЧЕСКИ ВАЖНО: Задача считается выполненной ТОЛЬКО после реального тестирования на фронтенде!**

**Перед тем как заявлять, что задача выполнена и решение «готово и работает», ты ОБЯЗАН:**

**🚨 ПЕРВЫМ ДЕЛОМ - ПРОВЕРКА НА ФРОНТЕНДЕ (ОБЯЗАТЕЛЬНО!):**
1. **ОБЯЗАТЕЛЬНО запустить Frontend** (если не запущен)
2. **ОБЯЗАТЕЛЬНО открыть изменённую страницу в браузере**
3. **ОБЯЗАТЕЛЬНО выполнить действия, которые используют изменённую функциональность**
4. **ОБЯЗАТЕЛЬНО убедиться, что результат запускается и отображается так, как это задумано (И ОЖИДАЕТСЯ)**
5. **ОБЯЗАТЕЛЬНО проверить консоль браузера (F12) - не должно быть ошибок**
6. **ОБЯЗАТЕЛЬНО проверить Network tab - запросы должны быть успешными**

**ЭТО ПРАВИЛО НЕ МОЖЕТ БЫТЬ ИГНОРИРОВАНО!**

**Затем выполнить остальные проверки:**

1. Явно перечислить, какие файлы ты изменил и зачем
2. **Проверить импорты** (для Backend): запустить `python temp/backend/check_imports.py`
3. **Использовать инструменты автоматизации:**
   - **Запустить `.\scripts\quick-check.ps1 all`** перед завершением задачи
   - Если проверка не прошла — исправить ошибки ПЕРЕД завершением задачи
4. **ОБЯЗАТЕЛЬНОЕ ТЕСТИРОВАНИЕ (ВЫПОЛНЯЕТСЯ ВСЕГДА, БЕЗ ИСКЛЮЧЕНИЙ):**

   **4.1. Если изменены скрипты запуска (.bat, .ps1):**
   - **ОБЯЗАТЕЛЬНО запустить сервисы через изменённый скрипт:**
     ```powershell
     # Для start-all-tabby.bat или start-all-services-single-window.ps1:
     .\start-all-tabby.bat
     # Или:
     .\scripts\start-all-services-single-window.ps1 -ProjectRoot "D:\tryagain" -Mode "debug"
     ```
   - **ОБЯЗАТЕЛЬНО проверить, что все сервисы запустились:**
     ```powershell
     # Проверить health endpoints:
     Invoke-WebRequest http://127.0.0.1:8000/health  # Backend
     Invoke-WebRequest http://127.0.0.1:9003/health  # Parser Service
     Invoke-WebRequest http://localhost:3000         # Frontend
     ```
   - **ОБЯЗАТЕЛЬНО проверить логи на наличие ошибок:**
     ```powershell
     Get-Content "logs\Backend-*.log" -Tail 30
     Get-Content "logs\Frontend-*.log" -Tail 30
     Get-Content "logs\Parser Service-*.log" -Tail 30
     ```
   - **НЕ заявлять о завершении, если сервисы не запускаются или есть ошибки в логах!**

   **4.2. Если изменены пути, импорты, структура файлов:**
   - **ОБЯЗАТЕЛЬНО запустить сервисы и проверить работоспособность** (см. 4.1)
   - **ОБЯЗАТЕЛЬНО проверить, что нет ошибок импорта/модулей:**
     ```powershell
     # Backend:
     cd backend
     python -c "import app.main; print('OK: Backend imports work')"
     
     # Frontend:
     cd frontend/moderator-dashboard-ui
     npm run type-check
     ```

   **4.3. Если изменён Backend (роутеры, usecases, схемы):**
   - **ОБЯЗАТЕЛЬНО запустить Backend и проверить:**
     ```powershell
     # Запустить Backend (если не запущен):
     cd backend
     uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
     
     # В другом терминале проверить health:
     Invoke-WebRequest http://127.0.0.1:8000/health
     
     # Проверить изменённый endpoint (если применимо):
     # Например, для GET /moderator/suppliers:
     Invoke-WebRequest http://127.0.0.1:8000/moderator/suppliers
     ```
   - **ОБЯЗАТЕЛЬНО проверить через Frontend** (если endpoint используется на Frontend):
     - Открыть страницу, которая использует изменённый endpoint
     - Выполнить операцию (создание, обновление, удаление и т.д.)
     - **ОБЯЗАТЕЛЬНО убедиться, что результат запускается и отображается так, как это задумано (И ОЖИДАЕТСЯ)**
     - Убедиться, что всё работает корректно
     - Проверить консоль браузера (F12) - не должно быть ошибок

   **4.4. Если изменён Frontend (компоненты, страницы, API клиент):**
   - **ОБЯЗАТЕЛЬНО запустить Frontend и проверить:**
     ```powershell
     # Запустить Frontend (если не запущен):
     cd frontend/moderator-dashboard-ui
     npm run dev
     
     # Проверить доступность:
     Invoke-WebRequest http://localhost:3000
     ```
   - **ОБЯЗАТЕЛЬНО открыть изменённую страницу в браузере:**
     - Открыть `http://localhost:3000` и перейти на изменённую страницу
     - Выполнить действия, которые используют изменённую функциональность
     - **ОБЯЗАТЕЛЬНО убедиться, что результат запускается и отображается так, как это задумано (И ОЖИДАЕТСЯ)**
     - Проверить консоль браузера (F12) - не должно быть ошибок
     - Проверить Network tab - запросы должны быть успешными
     - Убедиться, что UI работает корректно
   
   **⚠️ ВАЖНО: Это правило применяется ТАКЖЕ, если изменён Backend API, который используется на Frontend!**

   **4.5. Если изменена структура БД или миграции:**
   - **ОБЯЗАТЕЛЬНО применить миграцию и проверить:**
     ```powershell
     # Применить миграцию:
     $env:PGPASSWORD="your_password"
     psql -U postgres -d your_database -h localhost -p 5432 -f backend\migrations\XXX_migration.sql
     
     # Проверить структуру:
     psql -U postgres -d your_database -h localhost -p 5432 -c "\d table_name"
     ```
   - **ОБЯЗАТЕЛЬНО запустить Backend и проверить работу с БД:**
     - Запустить Backend
     - Выполнить операцию, которая использует изменённую таблицу
     - Проверить, что данные сохраняются/читаются корректно

5. **Обновить документацию:**
   - Добавить ошибку в `docs/TROUBLESHOOTING.md` (если новая)
   - Обновить `docs/PROJECT_SPECIFICATION.md` ТОЛЬКО если исправление проверено и работает
   - **ОБЯЗАТЕЛЬНО обновить `docs/PROJECT_PATTERNS.md`** если:
     - Реализован новый паттерн
     - Исправлена проблемная структура
     - Найден правильный способ решения задачи
6. Привести точные команды для проверки
7. Описать, какой результат должен увидеть пользователь

**ЗАПРЕЩЕНО:**
- Заявлять, что задача завершена, не выполнив тестирование из пункта 4
- Говорить "должно работать" или "логика правильная" без реальной проверки
- Просить пользователя протестировать изменения вместо самостоятельного тестирования
- Пропускать тестирование, даже если изменения кажутся "незначительными"

**ПРИМЕР ПРАВИЛЬНОГО ЗАВЕРШЕНИЯ ЗАДАЧИ:**

❌ НЕПРАВИЛЬНО:
"Задача выполнена. Реализовано автоматическое извлечение ИНН при выборе домена."

✅ ПРАВИЛЬНО:
"Задача выполнена. Проверено через фронтенд:
1. Открыл страницу http://localhost:3000/parsing-runs/123
2. Выбрал домен example.com через чекбокс
3. Увидел индикатор 'Извлечение...'
4. Через несколько секунд увидел желтый бейдж 'ИНН: 1234567890' с ссылкой
5. Кликнул на бейдж - открылась страница с пруфом
6. Проверил консоль - нет ошибок
7. Проверил Network tab - запрос к /inn-extraction/extract-batch успешен (200)

Результат: функциональность работает как задумано. ИНН автоматически извлекается при выборе домена и отображается желтым цветом с рабочей ссылкой на пруф."

**⚠️ КРИТИЧЕСКИ ВАЖНО: Проверка импортов**

После ЛЮБОГО изменения файлов роутеров или usecases ОБЯЗАТЕЛЬНО:
1. Запустить `python temp/backend/check_imports.py`
2. Если есть ошибки — исправить их
3. Только после этого заявлять, что задача завершена

## Правила проверки влияния изменений

**⚠️ КРИТИЧЕСКИ ВАЖНО: Перед изменением функциональности, которая используется в нескольких местах, ОБЯЗАТЕЛЬНО выполни эти проверки.**

**Перед изменением функциональности, которая используется в нескольких местах:**

1. **ОБЯЗАТЕЛЬНО прочитай `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md`** для затронутой функциональности
2. **ОБЯЗАТЕЛЬНО найди все места использования** через поиск по кодовой базе
3. **ОБЯЗАТЕЛЬНО используй единый подход** для реализации в разных местах
4. **ОБЯЗАТЕЛЬНО проверь все места использования** после изменений

**После изменений:**

1. **ОБЯЗАТЕЛЬНО выполни чеклист из `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md` раздел "Часть 2: Чеклисты проверки изменений"**
2. **ОБЯЗАТЕЛЬНО проверь все страницы**, где используется измененная функциональность
3. **ОБЯЗАТЕЛЬНО проверь кэширование**, если оно используется
4. **ОБЯЗАТЕЛЬНО проверь автоматическое обновление**, если оно используется

**Документация:** `docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md`

## Изменение существующих файлов (Safe Editing)

При работе с ВАЖНЫМИ файлами (роуты, usecases, adapters, db‑модели, api.ts, types.ts) действую ТАК:

1. Сначала ЧИТАЮ файл и КРАТКО формулирую, что он делает
2. **Проверяю все импорты** — убеждаюсь, что все используемые типы и классы импортированы
3. В ответе описываю ПЛАН изменений (списком), не меняя код
4. ЖДУ подтверждения пользователя на план, если изменения затрагивают:
   - структуру БД
   - существующие API‑эндпоинты
   - общие типы DTO
   - ключевые компоненты фронта

**После внесения изменений:**
- Проверяю, что все необходимые импорты присутствуют
- Запускаю `python temp/backend/check_imports.py` (для Backend)
- Убеждаюсь, что код компилируется без ошибок

Запрещено:
- Переписывать файл «целиком с нуля», если можно внести локальные изменения
- Удалять блоки кода, если не уверен, что они не используются
- Переименовывать функции/классы/поля, не проверив все места использования
- **Удалять или не добавлять необходимые импорты** при редактировании файла

## Правила работы с импортами (критически важно!)

**⚠️ Эти правила предотвращают ошибки типа "NameError: name 'date' is not defined"**

### При редактировании файлов

1. **Перед началом редактирования:**
   - Проверяю, что все используемые типы и классы импортированы
   - Если вижу использование типа без импорта — добавляю импорт СРАЗУ

2. **При копировании кода:**
   - **ОБЯЗАТЕЛЬНО копирую блок импортов вместе с кодом**
   - Если копирую функцию, использующую `date` — копирую и `from datetime import date`

3. **При рефакторинге:**
   - **НЕ удаляю импорты**, которые используются в коде
   - Перед удалением импорта проверяю, что он нигде не используется (grep по файлу)

### Перед завершением задачи

**ОБЯЗАТЕЛЬНО:**
1. Запустить `python temp/backend/check_imports.py`
2. Если есть ошибки — исправить их ПЕРЕД завершением задачи
3. Убедиться, что код компилируется без ошибок

## Использование инструментов автоматизации

**⚠️ КРИТИЧЕСКИ ВАЖНО: Ты ОБЯЗАН использовать созданные инструменты автоматизации!**

**Перед завершением ЛЮБОЙ задачи ты ОБЯЗАН:**

1. **Запустить быструю проверку сервисов:**
   ```powershell
   .\scripts\quick-check.ps1 all
   ```
   - Это проверяет импорты Backend, линтер Frontend, health endpoints
   - Если проверка не прошла — исправить ошибки ПЕРЕД завершением задачи
   - **НЕ заявлять, что задача завершена, если quick-check не прошел**

2. **При документировании ошибок:**
   - Использовать шаблон `temp/templates/troubleshooting_template.md`
   - Заполнить все поля шаблона
   - Скопировать заполненный шаблон в `docs/TROUBLESHOOTING.md`

**Документация:** `docs/MASTER_INSTRUCTION.md` - раздел "5. Инструменты автоматизации"

## Использование терминала

**⚠️ КРИТИЧЕСКИ ВАЖНО: Ты ОБЯЗАН выполнять все проверки и тесты самостоятельно через терминал!**

**🚨 КРИТИЧЕСКИ ВАЖНО: Проверка через фронтенд - это НЕ опция, это ОБЯЗАТЕЛЬНОСТЬ!**

**Если задача затрагивает Frontend или Backend API, ты ОБЯЗАН:**

1. **ОБЯЗАТЕЛЬНО открыть страницу в браузере** (не просто проверить HTTP статус)
2. **ОБЯЗАТЕЛЬНО выполнить use-case** (не просто проверить, что код компилируется)
3. **ОБЯЗАТЕЛЬНО увидеть результат** (не просто проверить, что нет ошибок в консоли)

**НЕ ДОСТАТОЧНО:**
- ❌ Проверить, что Frontend отвечает (HTTP 200)
- ❌ Проверить, что код компилируется
- ❌ Проверить, что нет ошибок TypeScript
- ❌ Проверить, что endpoint доступен

**ДОСТАТОЧНО ТОЛЬКО:**
- ✅ Открыл страницу в браузере
- ✅ Выполнил действия
- ✅ Увидел результат на экране
- ✅ Убедился, что результат работает как задумано

**ОБЯЗАТЕЛЬНО:**

- **Всегда, когда для решения задачи можно что‑то проверить или выполнить через терминал**, **ВЫПОЛНЯЙ эти команды САМ через терминал, а не проси пользователя сделать это**

- **После ЛЮБЫХ изменений в коде ОБЯЗАТЕЛЬНО:**
  1. **ОБЯЗАТЕЛЬНО запустить `.\scripts\quick-check.ps1 all`** для проверки всех сервисов
  2. Запустить все необходимые скрипты проверки через свое окружение (терминал)
  3. Убедиться, что код компилируется без ошибок
  4. **ОБЯЗАТЕЛЬНО запустить и протестировать изменённую функциональность:**
     - Если изменены скрипты запуска → запустить сервисы и проверить health endpoints
     - Если изменён Backend → запустить Backend, проверить через API, открыть Frontend и проверить работу
     - Если изменён Frontend → запустить Frontend, открыть в браузере, проверить функциональность
     - Если изменена структура БД → применить миграцию, проверить работу с БД
  5. **ОБЯЗАТЕЛЬНО проверить логи на наличие ошибок:**
     ```powershell
     Get-Content "logs\Backend-*.log" -Tail 30
     Get-Content "logs\Frontend-*.log" -Tail 30
     ```
  6. Только после этого заявлять, что задача завершена

**ЗАПРЕЩЕНО:**

- Просить пользователя выполнить команды для проверки изменений
- Просить пользователя открыть страницы фронтенда для тестирования
- Заявлять, что «всё готово» или «должно работать», если ты мог выполнить проверку через терминал, но не сделал этого
- Заявлять о завершении задачи без реального тестирования изменённой функциональности
- Пропускать тестирование, даже если изменения кажутся "незначительными" или "очевидными"

## Проверка гипотез через фронтенд

**⚠️ КРИТИЧЕСКИ ВАЖНО: Все гипотезы о работе функциональности ОБЯЗАТЕЛЬНО проверяются через реальный фронтенд!**

При диагностике проблем или проверке исправлений:

1. **НЕ предполагай, что решение работает** - всегда проверяй через фронтенд
2. **Используй реальные данные** - не тестовые, а те, что есть в системе
3. **Выполняй реальные сценарии** - те, что использует пользователь
4. **Если гипотеза не подтвердилась** - ищи другую причину, не предполагай дальше

**ОБЯЗАТЕЛЬНОЕ ТЕСТИРОВАНИЕ ЧЕРЕЗ ФРОНТЕНД:**

После ЛЮБЫХ изменений, затрагивающих Frontend или Backend API:

1. **Запустить все сервисы:**
   ```powershell
   .\start-all-tabby.bat
   # Или через PowerShell:
   .\scripts\start-all-services-single-window.ps1 -ProjectRoot "D:\tryagain" -Mode "debug"
   ```

2. **Дождаться запуска всех сервисов** (проверить health endpoints)

3. **Открыть Frontend в браузере:**
   - Открыть `http://localhost:3000`
   - Перейти на изменённую страницу
   - Выполнить действия, которые используют изменённую функциональность

4. **Проверить консоль браузера (F12):**
   - Нет ошибок JavaScript
   - Нет ошибок в Network tab
   - Все запросы успешны (статус 200, 201, 204)

5. **Проверить логи Backend:**
   ```powershell
   Get-Content "logs\Backend-*.log" -Tail 50
   ```
   - Нет ошибок импорта
   - Нет ошибок выполнения
   - Запросы обрабатываются корректно

**Запрещено:**
- Заявлять, что решение работает, не проверив через фронтенд
- Предполагать, что логика правильная, не проверив реально
- Говорить "должно работать" или "логика правильная" без реальной проверки
- Заявлять о завершении задачи, не выполнив тестирование через фронтенд

## Мониторинг состояния системы

**⚠️ КРИТИЧЕСКИ ВАЖНО: При диагностике проблем ОБЯЗАТЕЛЬНО проверяй состояние системы!**

**Порядок диагностики:**

1. **СНАЧАЛА проверь состояние системы (ОБЯЗАТЕЛЬНО!):**
   - **Используй чеклист из `docs/TROUBLESHOOTING.md` раздел "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ"**
   - Выполни ВСЕ команды из чеклиста
   - **НЕ предлагай решения, пока не выполнил эти проверки!**

2. **ЗАТЕМ ищи решение:**
   - Проверь `docs/TROUBLESHOOTING.md` на наличие похожих проблем
   - Используй раздел "Рекомендации для быстрой диагностики проблем" из `docs/TROUBLESHOOTING.md`
   - Изучи логи для понимания причины ошибки

3. **ПОСЛЕ исправления:**
   - Проверь, что все сервисы работают корректно (повтори проверки из пункта 1)
   - Убедись, что проблема решена через повторную проверку состояния
   - Задокументируй решение в `docs/TROUBLESHOOTING.md`

**Документация:** `docs/TROUBLESHOOTING.md` - раздел "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ"

## Правило: Структура перед функционалом

**⚠️ КРИТИЧЕСКИ ВАЖНО: При добавлении нового функционала СНАЧАЛА проверь и исправь структуру!**

**При добавлении нового функционала:**

1. **СНАЧАЛА проверь структуру:**
   - Правильная ли структура компонента?
   - Нет ли проблемных паттернов (button в button, a в a)?
   - Соответствует ли структура паттернам из `docs/PROJECT_PATTERNS.md`?

2. **ЕСЛИ структура проблемная:**
   - **Исправить структуру СРАЗУ**
   - **НЕ добавлять функционал в проблемную структуру**
   - Использовать правильные паттерны из `docs/PROJECT_PATTERNS.md`

3. **ТОЛЬКО ПОСЛЕ исправления структуры:**
   - Добавлять новый функционал
   - Проверять на фронтенде
   - Заявлять о завершении

**Пример:**
- ❌ НЕПРАВИЛЬНО: Добавить ИНН в AccordionTrigger, где уже есть Checkbox (проблемная структура)
- ✅ ПРАВИЛЬНО: Сначала вынести Checkbox из AccordionTrigger, потом добавить ИНН

---

## Правило: Поиск паттернов перед изменениями

**⚠️ КРИТИЧЕСКИ ВАЖНО: Перед изменением компонента, который используется в нескольких местах, ОБЯЗАТЕЛЬНО найти правильные паттерны!**

**Перед изменением компонента:**

1. **Найти все места использования:**
   ```powershell
   codebase_search "Where is ComponentName used?"
   grep -r "ComponentName" frontend/ backend/
   ```

2. **Изучить правильные реализации:**
   - Посмотреть на все файлы, где используется компонент
   - Выбрать ПРАВИЛЬНЫЙ паттерн (не первый попавшийся)
   - Использовать этот паттерн как образец

3. **Проверить комментарии:**
   - Искать комментарии о проблемах и исправлениях
   - Учитывать архитектурные решения
   - Обращать внимание на комментарии типа "вынесено для исправления", "fix", "workaround"

4. **Проверить `docs/PROJECT_PATTERNS.md`:**
   - Есть ли паттерн для этого компонента?
   - Какие примеры правильного использования?
   - Что запрещено?

---

## Правило: Документирование паттернов

**⚠️ КРИТИЧЕСКИ ВАЖНО: После реализации нового паттерна или исправления структуры ОБЯЗАТЕЛЬНО задокументировать!**

**После реализации нового паттерна или исправления структуры:**

1. **ОБЯЗАТЕЛЬНО добавить в `docs/PROJECT_PATTERNS.md`:**
   - Описание паттерна
   - Пример кода (правильный и неправильный)
   - Места использования в проекте
   - Что запрещено (анти-паттерны)

2. **Если исправил проблемную структуру:**
   - Указать, что было неправильно
   - Указать, как исправлено
   - Указать примеры правильного использования
   - Указать файлы, где паттерн используется

**Пример:**
- После исправления Checkbox + AccordionTrigger добавить в `docs/PROJECT_PATTERNS.md`:
  - Правильный паттерн
  - Примеры из `keywords/page.tsx` и `parsing-runs/[runId]/page.tsx`
  - Запрет на вложенные кнопки

---

## Документирование ошибок и решений (БИБЛИЯ)

**⚠️ КРИТИЧЕСКИ ВАЖНО: Все ошибки и их решения ОБЯЗАТЕЛЬНО документируются и являются "НЕПРИКОСНОВЕННЫМИ, КАК БИБЛИЯ".**

### Правила документирования:

1. **Каждая ошибка ДОЛЖНА быть задокументирована:**
   - В файле `docs/TROUBLESHOOTING.md` (основной документ "Библия ошибок")
   - С полным описанием проблемы, причины, решения и проверки
   - С указанием даты решения и измененных файлов

2. **Решения НЕ ДОЛЖНЫ быть удалены:**
   - Даже если кажется, что проблема больше не актуальна
   - Даже если код изменился и решение выглядит устаревшим
   - Решения могут быть помечены как "устаревшие", но НЕ удалены

3. **Перед решением новой ошибки:**
   - ОБЯЗАТЕЛЬНО проверь `docs/TROUBLESHOOTING.md`
   - Если похожая ошибка уже была решена - используй существующее решение
   - Если решение не подходит - документируй новое решение рядом со старым

4. **При документировании ошибок ОБЯЗАТЕЛЬНО:**
   - Использовать шаблон `temp/templates/troubleshooting_template.md`
   - Заполнить все поля шаблона
   - Скопировать заполненный шаблон в `docs/TROUBLESHOOTING.md`

**Документация:** `docs/TROUBLESHOOTING.md`

## Правила ведения документации

### 1. Запрет на раздувание файлов

- НЕЛЬЗЯ создавать новые .md/.txt в docs/ без явной необходимости
- Сначала ищи подходящий существующий раздел в:
  - docs/MASTER_INSTRUCTION.md
  - docs/TROUBLESHOOTING.md
  - docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md
  - docs/PROJECT_SPECIFICATION.md
  - docs/TASK_CHECKLISTS.md
- Если подходящее место есть — дописывай туда
- Новый файл можно создать только если:
  - тема действительно новая и будет использоваться регулярно
  - в начале файла есть блок "Назначение" и "Когда использовать"

### 2. Что обязательно фиксировать

- Любое изменение API или контрактов:
  - обновить docs/PROJECT_SPECIFICATION.md
  - при необходимости кратко упомянуть в MASTER_INSTRUCTION.md
- Любое изменение структуры БД или миграций:
  - обновить docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md (раздел миграций/последовательностей)
  - при необходимости обновить раздел БД в MASTER_INSTRUCTION.md
- Любое изменение критических связок backend–frontend–parser:
  - обновить docs/PROJECT_SPECIFICATION.md (рабочие связки)
  - при необходимости обновить docs/CRITICAL_INTEGRATIONS_AND_CHECKLISTS.md
- Любая новая проблема/ошибка:
  - добавить в docs/TROUBLESHOOTING.md по шаблону:
    описание → причина → решение → команды проверки

### 3. Фиксация изменений и перемещений файлов

**⚠️ КРИТИЧЕСКИ ВАЖНО: При перемещении файлов ОБЯЗАТЕЛЬНО обновлять все ссылки на них!**

**Перед перемещением файла/директории:**
1. **ОБЯЗАТЕЛЬНО найти все места использования:**
   ```powershell
   grep -r "filename" . --include="*.bat" --include="*.ps1" --include="*.py" --include="*.md" --include="*.ts" --include="*.tsx"
   ```
   Или через codebase_search: "Where is filename used?"

2. **ОБЯЗАТЕЛЬНО обновить все ссылки:**
   - В .bat файлах (пути к скриптам)
   - В .ps1 файлах (пути к скриптам)
   - В .py файлах (импорты, пути)
   - В документации (.md файлах)
   - В конфигурационных файлах

3. **ОБЯЗАТЕЛЬНО проверить внутренние пути в перемещаемом файле:**
   - **КРИТИЧЕСКИ ВАЖНО для .bat/.ps1 файлов:**
     - Проверить все команды `cd`, `call`, `start`, `pushd` с относительными путями
     - Проверить использование `%~dp0` (директория скрипта) - пути могут стать неверными
     - Если файл был в корне и использовал `%~dp0backend`, после перемещения в `scripts/` нужно `%~dp0..\backend`
     - Если файл вызывал другие скрипты через `%~dp0scripts\file.bat`, после перемещения в `scripts/` нужно `%~dp0file.bat`
   - **Для .py файлов:**
     - Проверить импорты (особенно относительные `from .module import`)
     - Проверить пути к файлам конфигурации, данных
   - **Для всех файлов:**
     - Проверить все относительные пути внутри файла

4. **ОБЯЗАТЕЛЬНО протестировать после перемещения:**
   - **Если перемещён .bat/.ps1 файл запуска:**
     - ОБЯЗАТЕЛЬНО запустить сервисы через изменённый скрипт:
       ```powershell
       .\start-all-tabby.bat
       # Или:
       .\scripts\start-all-services-single-window.ps1 -ProjectRoot "D:\tryagain" -Mode "debug"
       ```
     - ОБЯЗАТЕЛЬНО проверить health endpoints всех сервисов:
       ```powershell
       Invoke-WebRequest http://127.0.0.1:8000/health  # Backend
       Invoke-WebRequest http://127.0.0.1:9003/health  # Parser Service
       Invoke-WebRequest http://localhost:3000          # Frontend
       ```
     - ОБЯЗАТЕЛЬНО проверить логи на наличие ошибок:
       ```powershell
       Get-Content "logs\Backend-*.log" -Tail 30
       Get-Content "logs\Frontend-*.log" -Tail 30
       ```
     - НЕ заявлять о завершении, если сервисы не запускаются или есть ошибки в логах!
   - **Если перемещён другой файл:**
     - Запустить скрипты/код, которые используют перемещённый файл
     - Убедиться, что все ссылки работают корректно
   - **НЕ заявлять о завершении задачи, пока не протестировано!**

**После перемещения файлов/директорий:**
- Обновить docs/PROJECT_MAP.md (если затронута структура)
- При необходимости обновить раздел "Структура проекта" и "Архивная документация" в MASTER_INSTRUCTION.md
- При изменении структуры docs: корректировать список архивных файлов в конце MASTER_INSTRUCTION.md

**Запрещено:**
- Перемещать файлы без обновления всех ссылок на них
- Заявлять, что задача завершена, если не проверены все ссылки на перемещённые файлы

### 4. Критерий завершения задачи

Задача считается выполненной только если:
- код работает и прошёл проверки (scripts/quick-check.ps1 all, линтеры, check_imports и т.п.)
- обновлены соответствующие разделы документации
- добавлены или обновлены команды проверки (curl, скрипты, шаги во фронтенде), если менялся функционал
- новые ошибки, если были, задокументированы в TROUBLESHOOTING.md

## When Unsure

Если ты не уверен:
- где разместить файл
- как назвать endpoint
- как связаны слои

СНАЧАЛА задай уточняющие вопросы пользователю и НЕ пиши код, основанный на догадках.

## Git и страховка перед крупными изменениями

Перед любыми крупными изменениями (рефакторинг модулей, изменение схемы БД, массовые правки фронта):

- Убедись, что рабочее состояние закоммичено в Git
- Если в проекте ещё нет Git, предложи инициализировать его и настроить .gitignore
- Не выполняй опасные изменения, если в дереве есть незакоммиченные файлы, о которых пользователь ничего не говорил

---

**Последнее обновление:** 2025-12-29
